---
phase: 06-new-backends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - llm_benchmark/backends/llamacpp.py
  - llm_benchmark/backends/lmstudio.py
  - tests/test_llamacpp.py
  - tests/test_lmstudio.py
  - tests/conftest.py
  - pyproject.toml
autonomous: true
requirements: [BEND-01, BEND-02]

must_haves:
  truths:
    - "LlamaCppBackend satisfies Backend Protocol and converts ms timings to seconds"
    - "LMStudioBackend satisfies Backend Protocol and derives timing from tokens_per_second"
    - "Both backends use httpx for HTTP communication, not raw urllib"
    - "Both backends raise BackendError on failures with retryable flag"
  artifacts:
    - path: "llm_benchmark/backends/llamacpp.py"
      provides: "LlamaCppBackend class"
      exports: ["LlamaCppBackend"]
      min_lines: 150
    - path: "llm_benchmark/backends/lmstudio.py"
      provides: "LMStudioBackend class"
      exports: ["LMStudioBackend"]
      min_lines: 120
    - path: "tests/test_llamacpp.py"
      provides: "LlamaCppBackend unit tests"
      min_lines: 80
    - path: "tests/test_lmstudio.py"
      provides: "LMStudioBackend unit tests"
      min_lines: 80
  key_links:
    - from: "llm_benchmark/backends/llamacpp.py"
      to: "llm_benchmark/backends/__init__.py"
      via: "imports BackendResponse, BackendError, StreamResult"
      pattern: "from llm_benchmark\\.backends import"
    - from: "llm_benchmark/backends/lmstudio.py"
      to: "llm_benchmark/backends/__init__.py"
      via: "imports BackendResponse, BackendError, StreamResult"
      pattern: "from llm_benchmark\\.backends import"
---

<objective>
Implement LlamaCppBackend and LMStudioBackend core classes with full Backend Protocol compliance and comprehensive unit tests.

Purpose: These are the two new backend implementations that enable benchmarking on llama-server and LM Studio. They are the foundation for all Phase 6 integration work.
Output: Two backend classes with httpx HTTP clients, timing conversion, model management, and full test coverage via mocked httpx responses.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-new-backends/06-CONTEXT.md
@.planning/phases/06-new-backends/06-RESEARCH.md

<interfaces>
<!-- Backend Protocol and types the new backends must implement -->

From llm_benchmark/backends/__init__.py:
```python
@runtime_checkable
class Backend(Protocol):
    @property
    def name(self) -> str: ...
    @property
    def version(self) -> str: ...
    def chat(self, model: str, messages: list[dict[str, str]], stream: bool = False, options: dict[str, Any] | None = None) -> BackendResponse | StreamResult: ...
    def list_models(self) -> list[dict[str, Any]]: ...
    def unload_model(self, model: str) -> bool: ...
    def warmup(self, model: str, timeout: float) -> bool: ...
    def detect_context_window(self, model: str) -> int: ...
    def get_model_size(self, model: str) -> float | None: ...
    def check_connectivity(self) -> bool: ...

class BackendResponse(BaseModel):
    model: str
    content: str
    done: bool
    prompt_eval_count: int = 0
    eval_count: int = 0
    total_duration: float = 0.0
    load_duration: float = 0.0
    prompt_eval_duration: float = 0.0
    eval_duration: float = 0.0
    prompt_cached: bool = False

class BackendError(Exception):
    def __init__(self, message: str, retryable: bool = False, original: Exception | None = None): ...

class StreamResult:
    def __init__(self, chunks: Iterator[str], finalize: Any): ...
    @property
    def chunks(self) -> Iterator[str]: ...
    @property
    def response(self) -> BackendResponse: ...
```

From llm_benchmark/backends/ollama.py (reference pattern):
- Uses SDK-specific client for HTTP calls
- _to_response() converts native timing format to BackendResponse (seconds)
- Wraps SDK exceptions in BackendError with retryable flag
- _handle_stream() returns StreamResult with chunk_generator + finalize
- list_models() returns list[dict] with at minimum 'model' and 'size' keys
- unload_model() specific to backend (Ollama uses keep_alive=0)
- check_connectivity() returns bool (True if server responds)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LlamaCppBackend and LMStudioBackend</name>
  <files>llm_benchmark/backends/llamacpp.py, llm_benchmark/backends/lmstudio.py, tests/test_llamacpp.py, tests/test_lmstudio.py, tests/conftest.py, pyproject.toml</files>
  <behavior>
    LlamaCppBackend:
    - isinstance(LlamaCppBackend(), Backend) returns True
    - name property returns "llama-cpp"
    - chat() sends POST to /v1/chat/completions, returns BackendResponse with timings converted from ms to seconds
    - chat() with mock response {timings: {prompt_ms: 30.0, predicted_ms: 660.0, prompt_n: 10, predicted_n: 35}} produces BackendResponse with prompt_eval_duration=0.03, eval_duration=0.66, prompt_eval_count=10, eval_count=35
    - chat(stream=True) returns StreamResult; iterating chunks yields content; .response has timing data from final SSE chunk
    - list_models() is not applicable (llama-server serves one model) -- returns list with single entry based on /v1/models or /props endpoint
    - unload_model() terminates server process if managed, returns True
    - check_connectivity() sends GET /health, returns True on 200
    - warmup() sends a short chat request, returns True on success
    - detect_context_window() reads from /props or /v1/models endpoint, defaults to 4096
    - get_model_size() returns None (not available via API for llama-server)
    - All httpx errors wrapped in BackendError with retryable=True for ConnectError
    - Constructor accepts host, port parameters with defaults ("127.0.0.1", 8080)
    - httpx import is inside the class file, not in backends/__init__.py

    LMStudioBackend:
    - isinstance(LMStudioBackend(), Backend) returns True
    - name property returns "lm-studio"
    - chat() sends POST to /v1/chat/completions, returns BackendResponse with timing derived from stats.tokens_per_second
    - chat() with mock response {usage: {prompt_tokens: 10, completion_tokens: 35}, stats: {tokens_per_second: 45.2}} produces eval_count=35, eval_duration=35/45.2
    - chat(stream=True) returns StreamResult with accumulated chunks and final timing
    - list_models() queries /api/v1/models for full catalog, returns list[dict] with 'model' key
    - unload_model() sends POST /api/v1/models/unload, returns True on success
    - check_connectivity() sends GET /v1/models, returns True on 200
    - warmup() sends a short chat request, returns True on success
    - detect_context_window() defaults to 4096 (not reliably exposed by LM Studio)
    - get_model_size() returns None (not available in list response)
    - All httpx errors wrapped in BackendError
    - Constructor accepts host, port parameters with defaults ("127.0.0.1", 1234)
  </behavior>
  <action>
    First, add httpx as an explicit dependency: `uv add httpx`.

    Then write tests in tests/test_llamacpp.py and tests/test_lmstudio.py that mock httpx.Client responses. Add shared fixtures to tests/conftest.py for mock HTTP response factories.

    Then implement:

    **llm_benchmark/backends/llamacpp.py:**
    - Create LlamaCppBackend class with `__init__(self, host="127.0.0.1", port=8080)`
    - Use `httpx.Client(base_url=f"http://{host}:{port}", timeout=600.0)` stored as `self._client`
    - Store `self._server_process: subprocess.Popen | None = None` for managed server lifecycle (used by detection/auto-start in Plan 02)
    - `chat()`: POST to `/v1/chat/completions` with `{"model": model, "messages": messages, "stream": stream}`. For non-streaming, parse timings object: `prompt_ms/1000 -> prompt_eval_duration`, `predicted_ms/1000 -> eval_duration`, `prompt_n -> prompt_eval_count`, `predicted_n -> eval_count`. For streaming, implement SSE line parsing -- accumulate content chunks, extract timings from final data chunk
    - `_to_response(data, timings)`: Build BackendResponse with ms-to-seconds conversion
    - `list_models()`: GET `/v1/models` -- llama-server returns loaded model info. Return as list with single dict `{"model": name, "size": 0}`
    - `unload_model()`: If `self._server_process` exists, terminate and wait(10). Return True
    - `check_connectivity()`: GET `/health`, return True if status 200 and body contains `"ok"`
    - `warmup()`: Send a short chat request, catch errors, return bool
    - `detect_context_window()`: GET `/props` or parse from `/v1/models`, extract context size, default 4096
    - `get_model_size()`: Return None (not available from llama-server API)
    - `version` property: GET `/props`, extract "build" info, fallback to running `llama-server --version` via subprocess, fallback to "Unknown"
    - Wrap all httpx errors (ConnectError, TimeoutException, HTTPStatusError) in BackendError

    **llm_benchmark/backends/lmstudio.py:**
    - Create LMStudioBackend class with `__init__(self, host="127.0.0.1", port=1234)`
    - Use `httpx.Client(base_url=f"http://{host}:{port}", timeout=600.0)` stored as `self._client`
    - `chat()`: POST to `/v1/chat/completions` with `{"model": model, "messages": messages, "stream": stream}`. Parse `usage` for token counts, `stats` for `tokens_per_second`. Derive `eval_duration = eval_count / tps` if tps > 0
    - `_to_response(data)`: Build BackendResponse from usage + stats fields with defensive `.get()` access
    - `list_models()`: GET `/api/v1/models` for full downloaded catalog. Map each to `{"model": m["id"], "size": 0}`
    - `unload_model()`: POST `/api/v1/models/unload` with `{"model": model}`. Return True on success, False on error
    - `check_connectivity()`: GET `/v1/models`, return True on 200
    - `warmup()`: Send a short chat request, catch errors, return bool
    - `detect_context_window()`: Default 4096
    - `get_model_size()`: Return None
    - `version` property: Try `lms version` subprocess, parse output, fallback to "Unknown"
    - Wrap all httpx errors in BackendError

    **Key patterns (from OllamaBackend reference):**
    - Do NOT import httpx at module level in `backends/__init__.py` -- import inside each backend file only
    - Use defensive `.get()` for all response field access (LM Studio stats fields are MEDIUM confidence)
    - StreamResult: create chunk_generator() that yields content strings, finalize() that builds BackendResponse from final timing data

    **conftest.py additions:**
    - Add `mock_httpx_response` fixture factory that creates mock httpx.Response objects with configurable status_code, json data, and headers
    - Add `llamacpp_chat_response` fixture with sample llama.cpp /v1/chat/completions JSON including timings object
    - Add `lmstudio_chat_response` fixture with sample LM Studio /v1/chat/completions JSON including usage + stats
  </action>
  <verify>
    <automated>uv run pytest tests/test_llamacpp.py tests/test_lmstudio.py -x -v</automated>
  </verify>
  <done>
    - LlamaCppBackend and LMStudioBackend classes exist and satisfy Backend Protocol (isinstance check passes)
    - All timing conversions verified: llama.cpp ms->seconds, LM Studio tokens_per_second->duration
    - Both backends raise BackendError on connection/HTTP failures
    - httpx added to pyproject.toml as explicit dependency
    - All 170 existing tests still pass alongside new tests
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/ -x -q
# Must show 170+ existing tests pass plus new backend tests
# Verify Protocol compliance:
# python -c "from llm_benchmark.backends import Backend; from llm_benchmark.backends.llamacpp import LlamaCppBackend; assert isinstance(LlamaCppBackend(), Backend)"
# python -c "from llm_benchmark.backends import Backend; from llm_benchmark.backends.lmstudio import LMStudioBackend; assert isinstance(LMStudioBackend(), Backend)"
</verification>

<success_criteria>
- LlamaCppBackend implements all Backend Protocol methods with correct ms-to-seconds timing conversion
- LMStudioBackend implements all Backend Protocol methods with tokens_per_second-derived timing
- Both classes use httpx.Client (not raw urllib or requests)
- Both raise BackendError wrapping httpx exceptions
- Tests cover chat (non-streaming + streaming), list_models, unload_model, check_connectivity, error handling
- httpx is an explicit dependency in pyproject.toml
- All 170+ existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/06-new-backends/06-01-SUMMARY.md`
</output>
