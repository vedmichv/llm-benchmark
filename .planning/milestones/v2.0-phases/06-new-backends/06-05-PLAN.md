---
phase: 06-new-backends
plan: 05
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - llm_benchmark/menu.py
  - llm_benchmark/cli.py
  - tests/test_menu.py
autonomous: true
requirements: [CLI-02, CLI-05]

must_haves:
  truths:
    - "Menu shows backend selection BEFORE mode selection (Quick/Standard/Full/Custom)"
    - "When only Ollama detected: still shows backend prompt with auto-select and note 'Only Ollama detected'"
    - "When multiple backends detected: shows numbered list with status (running/installed/not found)"
    - "Selected backend is used for preflight and benchmarking"
    - "Failure summary shown at end of benchmark with all skipped models and their errors"
  artifacts:
    - path: "llm_benchmark/menu.py"
      provides: "Interactive menu with backend selection step"
      contains: "backend"
      min_lines: 200
    - path: "llm_benchmark/cli.py"
      provides: "No-args path uses menu backend selection"
      contains: "run_interactive_menu"
  key_links:
    - from: "llm_benchmark/menu.py"
      to: "llm_benchmark/backends/detection.py"
      via: "detect_backends() for backend selection display"
      pattern: "detect_backends"
    - from: "llm_benchmark/menu.py"
      to: "llm_benchmark/backends/__init__.py"
      via: "create_backend() with selected backend name"
      pattern: "create_backend"
    - from: "llm_benchmark/cli.py"
      to: "llm_benchmark/menu.py"
      via: "run_interactive_menu returns namespace with backend field"
      pattern: "run_interactive_menu"
---

<objective>
Add backend selection to the interactive menu and update the CLI no-args path to use the selected backend. Complete the error handling UX with failure summaries.

Purpose: Students using the interactive menu (no CLI args) need to see and choose backends. This is the final integration that makes the multi-backend experience complete for interactive users.
Output: Interactive menu with backend selection, backend-aware no-args path, failure summary display.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-new-backends/06-CONTEXT.md
@.planning/phases/06-new-backends/06-01-SUMMARY.md
@.planning/phases/06-new-backends/06-02-SUMMARY.md
@.planning/phases/06-new-backends/06-03-SUMMARY.md
@.planning/phases/06-new-backends/06-04-SUMMARY.md

<interfaces>
From llm_benchmark/menu.py (current):
```python
def run_interactive_menu(backend, models: list) -> argparse.Namespace:
    # Currently receives backend already created
    # Needs to add backend selection step BEFORE mode selection
```

From llm_benchmark/cli.py (current no-args path, updated in Plan 03):
```python
if not args_list:
    backend = create_backend()  # Default ollama
    models = run_preflight_checks(backend=backend)
    args = run_interactive_menu(backend, models)
    return _handle_run(args)
```

From llm_benchmark/backends/detection.py (Plan 02):
```python
@dataclass
class BackendStatus:
    name: str
    installed: bool
    running: bool
    binary_path: str | None
    port: int
def detect_backends(port_overrides=None) -> list[BackendStatus]: ...
def auto_start_backend(backend_name, *, port=None, model_path=None, ...) -> subprocess.Popen: ...
```

From llm_benchmark/backends/__init__.py (updated in Plan 03):
```python
def create_backend(name="ollama", *, host=None, port=None) -> Backend: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend selection to interactive menu</name>
  <files>llm_benchmark/menu.py, tests/test_menu.py</files>
  <action>
    **Add _select_backend() function to menu.py:**

    ```python
    def _select_backend() -> tuple[str, int | None]:
        """Show backend selection and return (backend_name, port_override)."""
    ```

    - Call `detect_backends()` to get status list
    - Display numbered list with status indicators:
      ```
      Backends:
        1. ollama     [running]
        2. llama-cpp  [installed, not running]
        3. lm-studio  [not installed]
      ```
    - Color coding: running = green, installed = yellow, not installed = dim
    - If only one backend is detected (installed or running): auto-select it but still display the list with a note: "Only Ollama detected -- using Ollama. Install llama.cpp or LM Studio for more options."
    - If multiple backends installed/running: prompt "Select backend [1-N]: " using existing _prompt_choice()
    - If selected backend is installed but not running: prompt "Start {backend}? (y/N)" -- if yes, call auto_start_backend()
      - For llama-cpp: need to collect model_path first. Show GGUF file scanner or prompt for path
    - Return (backend_name, port_override_or_none)

    **Add GGUF model selection for llama-cpp in menu:**
    - When llama-cpp is selected in menu, call `_select_gguf_model() -> str` which:
      - Scans `~/.cache/huggingface/` recursively for *.gguf files (per CONTEXT.md decision)
      - Also accepts manual path input
      - Extracts model name from GGUF metadata if available (use simple struct-based parser from research), fallback to cleaned filename (strip .gguf, replace - with spaces)
      - Shows numbered list of found GGUF files with names
      - Returns selected path as string
    - Create a utility function `scan_gguf_files(directory: Path) -> list[tuple[Path, str]]` that returns (path, display_name) tuples
    - Create `extract_gguf_model_name(path: Path) -> str | None` for GGUF metadata parsing (from research code example)

    **Update run_interactive_menu() signature and flow:**
    - Change to: `run_interactive_menu() -> argparse.Namespace` (no longer receives backend and models -- it creates them internally)
    - Flow:
      1. Show header (LLM Benchmark title)
      2. Call _select_backend() -> (backend_name, port)
      3. Create backend via create_backend(backend_name, port=port)
      4. Run preflight (already happened in cli.py? -- need to coordinate)
      5. Show system summary
      6. Offer model downloads (existing)
      7. Show mode menu (existing Quick/Standard/Full/Custom)
      8. Return namespace with `backend` field added

    Actually, looking at the current flow in cli.py, preflight runs BEFORE menu. The menu currently receives `backend` and `models` already resolved. The cleanest approach:

    - Keep run_interactive_menu() receiving backend and models as now
    - Add a NEW function `select_backend_interactive() -> tuple[str, int | None, str | None]` that returns (backend_name, port, model_path)
    - Update cli.py no-args path to: call select_backend_interactive() first, then create_backend(), then preflight, then run_interactive_menu()

    **Update _build_namespace() to include backend field:**
    - Add `backend: str = "ollama"` parameter
    - Include in returned Namespace: `backend=backend`

    **Update cli.py no-args path:**
    - Import select_backend_interactive from menu
    - Call it first to get backend_name, port, model_path
    - Create backend with those params
    - Run preflight with that backend
    - Pass backend + models to run_interactive_menu() as before
    - The returned Namespace now includes `backend` field for _handle_run

    **Update _handle_run() in cli.py:**
    - If `args.backend` exists and is not "ollama", use it to create backend (already done in Plan 03)
    - Handle model_path: if llama-cpp backend, pass model_path to backend for server management

    **Error handling -- failure summary (per CONTEXT.md):**
    - In _handle_run(), replace the interactive "Continue with remaining models?" prompt with automatic skip + log
    - Track failures in a list: `failures: list[tuple[str, str]] = []` (model_name, error_message)
    - After benchmark loop, if failures: print failure summary table using Rich
    - Check known-issues hints from runner (Plan 04) and append to failure display

    **Update tests/test_menu.py:**
    - Test _select_backend() with various detect_backends() return values (mock)
    - Test single-backend auto-select behavior
    - Test multi-backend numbered selection
    - Test GGUF file scanning (mock Path.glob)
    - Test _build_namespace includes backend field
    - Test failure summary display
    - Ensure existing menu tests still pass
  </action>
  <verify>
    <automated>uv run pytest tests/test_menu.py tests/test_cli.py -x -v</automated>
  </verify>
  <done>
    - Menu shows backend selection BEFORE mode selection
    - Single-backend: auto-selects with awareness note
    - Multi-backend: numbered list with status, user picks
    - llama-cpp selection triggers GGUF file scanner
    - GGUF metadata extraction works (fallback to filename)
    - Backend field included in Namespace for _handle_run
    - Failure summary shown at end of benchmark (no interactive prompt per failure)
    - All 170+ existing tests pass alongside new menu tests
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/ -x -q
# All tests pass
# Full phase verification:
# python -m llm_benchmark run --help  (shows --backend, --port, --model-path)
# python -m llm_benchmark info  (shows backend inventory)
</verification>

<success_criteria>
- Interactive menu presents backend selection before mode selection
- When only Ollama detected: shows prompt with auto-select and informational note about alternatives
- When multiple backends detected: shows numbered list with running/installed/not-found status
- GGUF file scanner finds models in ~/.cache/huggingface/ for llama-cpp
- Backend name flows through to _handle_run via Namespace
- Model failures skip + log instead of interactive prompt; summary shown at end
- All 170+ existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-new-backends/06-05-SUMMARY.md`
</output>
