---
phase: 06-new-backends
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - llm_benchmark/exporters.py
  - llm_benchmark/system.py
  - llm_benchmark/runner.py
  - tests/test_exporters.py
  - tests/test_system.py
autonomous: true
requirements: [CLI-03, CLI-04]

must_haves:
  truths:
    - "Export filenames include backend name: benchmark_llama-cpp_20260314_120000.json"
    - "JSON metadata includes backend_name field"
    - "Markdown reports include backend name in header"
    - "System summary shows all detected backends with status (running/installed/not found)"
    - "`python -m llm_benchmark info` shows full backend inventory"
  artifacts:
    - path: "llm_benchmark/exporters.py"
      provides: "Backend-aware export filenames and metadata"
      contains: "backend"
    - path: "llm_benchmark/system.py"
      provides: "System summary with backend detection display"
      contains: "detect_backends"
  key_links:
    - from: "llm_benchmark/exporters.py"
      to: "llm_benchmark/models.py"
      via: "SystemInfo includes backend_name for export metadata"
      pattern: "backend_name"
    - from: "llm_benchmark/system.py"
      to: "llm_benchmark/backends/detection.py"
      via: "detect_backends() for system summary display"
      pattern: "from llm_benchmark\\.backends\\.detection import"
---

<objective>
Add backend name to export filenames, JSON metadata, and Markdown reports. Enhance system summary and `info` command to show all detected backends with their status.

Purpose: Users need to distinguish results from different backends, and the system summary should make backend availability visible at a glance.
Output: Backend-aware exports and system info display.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-new-backends/06-CONTEXT.md
@.planning/phases/06-new-backends/06-RESEARCH.md
@.planning/phases/06-new-backends/06-01-SUMMARY.md
@.planning/phases/06-new-backends/06-02-SUMMARY.md

<interfaces>
From llm_benchmark/exporters.py (current filename pattern):
```python
def _timestamp() -> str:
    return datetime.now().strftime("%Y%m%d_%H%M%S")
# Current filenames: benchmark_YYYYMMDD_HHMMSS.json
# Target:           benchmark_ollama_YYYYMMDD_HHMMSS.json
```

From llm_benchmark/models.py:
```python
class SystemInfo(BaseModel):
    cpu: str
    ram_gb: float
    gpu: str
    gpu_vram_gb: float | None
    os_name: str
    python_version: str
    backend_name: str      # Already exists
    backend_version: str   # Already exists
```

From llm_benchmark/system.py:
```python
def get_system_info(backend: Backend) -> SystemInfo: ...
def format_system_summary(backend: Backend) -> str: ...
```

From llm_benchmark/backends/detection.py (Plan 02):
```python
@dataclass
class BackendStatus:
    name: str
    installed: bool
    running: bool
    binary_path: str | None
    port: int
def detect_backends(port_overrides=None) -> list[BackendStatus]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend name to export filenames and metadata</name>
  <files>llm_benchmark/exporters.py, llm_benchmark/runner.py, tests/test_exporters.py</files>
  <action>
    **Update exporters.py filename generation:**
    - All export functions (export_json, export_csv, export_markdown, and their concurrent/sweep variants) already receive `system_info: SystemInfo` which has `backend_name`
    - Change filename pattern from `benchmark_{timestamp}` to `benchmark_{backend_name}_{timestamp}`
    - Use `system_info.backend_name` to get the backend name. Example: `benchmark_llama-cpp_20260314_120000.json`
    - For sweep exports: `sweep_{backend_name}_{timestamp}.json`
    - For concurrent exports: `concurrent_{backend_name}_{timestamp}.json`

    **Update export_json() metadata:**
    - The JSON output likely already has system_info serialized. Verify that `backend_name` and `backend_version` appear in the JSON. If system_info is serialized via `.model_dump()`, it should already be there. No change needed if so.
    - If JSON metadata is manually constructed, add `"backend": system_info.backend_name` and `"backend_version": system_info.backend_version`

    **Update export_markdown() report header:**
    - Add backend name to the Markdown report header, e.g.: `# Benchmark Results (llama-cpp)` or include in the system info section

    **Update runner.py error handling:**
    - In `benchmark_model()` and related functions, add known-issues hint table per CONTEXT.md decision:
      - Create a module-level `KNOWN_ISSUES` dict mapping `(backend_name, error_pattern_substring)` to human-readable hints
      - Example entries: `("ollama", "timeout"): "Known issue: try llama-cpp backend instead"`, `("llama-cpp", "connection refused"): "Is llama-server running? Start with: llama-server -m <model>"`
      - When a model fails, check if the error message matches any known issue pattern for the current backend, and append the hint to the error output
    - Add failure summary at end of benchmark: track skipped models in a list, print summary table after all models complete

    **Update tests/test_exporters.py:**
    - Test that export filenames include backend name (mock datetime for deterministic filenames)
    - Test JSON output contains backend_name in metadata
    - Test Markdown report mentions backend name
    - Ensure backward compat: existing tests still pass with backend_name="ollama" in SystemInfo
  </action>
  <verify>
    <automated>uv run pytest tests/test_exporters.py -x -v</automated>
  </verify>
  <done>
    - Export filenames follow pattern: benchmark_{backend}_{timestamp}.{ext}
    - JSON metadata includes backend_name and backend_version
    - Markdown report header shows backend name
    - Known-issues hint dict exists in runner with initial entries
    - Failure summary printed after benchmark with skipped models
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance system summary with backend inventory</name>
  <files>llm_benchmark/system.py, tests/test_system.py</files>
  <action>
    **Update format_system_summary() in system.py:**
    - After existing system info (CPU, RAM, GPU, OS), add a "Backends" section
    - Call `detect_backends()` from detection module (lazy import to avoid circular deps)
    - For each backend, show status with color coding:
      - Running: `[green]ollama (running, port 11434)[/green]`
      - Installed but not running: `[yellow]llama-cpp (installed, not running)[/yellow]`
      - Not found: `[dim]lm-studio (not installed)[/dim]`
    - Show the active backend prominently: `[bold]Active backend:[/bold] llama-cpp v0.1`
    - This fulfills CONTEXT.md: "System summary shows ALL detected backends with status"

    **Update _handle_info() context (cli.py touched in Plan 03, but system.py additions here):**
    - Add `get_backend_inventory() -> str` function to system.py that returns a rich-formatted string showing all backends with:
      - Name, installed status, running status, version (if running), port
      - Platform-specific install instructions for missing backends
    - This is called by _handle_info() after existing system info display

    **Update tests/test_system.py:**
    - Mock detect_backends() to return various combinations
    - Test format_system_summary() includes backend section
    - Test get_backend_inventory() output for all-installed, partial, none scenarios
    - Test that existing system info tests still pass
  </action>
  <verify>
    <automated>uv run pytest tests/test_system.py -x -v</automated>
  </verify>
  <done>
    - format_system_summary() shows all detected backends with color-coded status
    - get_backend_inventory() provides detailed backend inventory for info command
    - Active backend shown prominently in system summary
    - Tests cover various detection scenarios
    - All 170+ existing tests pass
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/ -x -q
# All tests pass
# Manual: python -m llm_benchmark info (should show backend inventory)
</verification>

<success_criteria>
- Export filenames include backend name between prefix and timestamp
- JSON metadata has backend_name field
- Markdown reports show backend name in header
- System summary displays all detected backends with running/installed/not-found status
- Info command shows full backend inventory with versions and ports
- Known-issues hint table provides actionable suggestions on common failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-new-backends/06-04-SUMMARY.md`
</output>
