---
phase: 04-student-experience
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - llm_benchmark/exporters.py
autonomous: true
requirements:
  - UX-06

must_haves:
  truths:
    - "Standard Markdown report includes a Rankings section with text bar chart between System Info and Summary"
    - "Markdown report has a compact one-line header with date, model count, and mode label"
    - "Markdown report includes a one-liner recommendation for best model"
    - "Concurrent Markdown report ranks models by aggregate throughput"
    - "Sweep Markdown report includes per-model best config callout"
  artifacts:
    - path: "llm_benchmark/exporters.py"
      provides: "Enhanced export_markdown, export_concurrent_markdown, export_sweep_markdown with rankings"
      exports: ["export_markdown", "export_concurrent_markdown", "export_sweep_markdown"]
  key_links:
    - from: "llm_benchmark/exporters.py"
      to: "llm_benchmark/display.py"
      via: "imports render_text_bar_chart for Markdown bar chart"
      pattern: "render_text_bar_chart"
---

<objective>
Enhance all three Markdown exporters (standard, concurrent, sweep) with rankings sections, text bar charts, and recommendations to create shareable reports.

Purpose: Students can share benchmark results with classmates via GitHub/Discord/Slack -- reports include visual rankings and hardware context.
Output: Enhanced export_markdown(), export_concurrent_markdown(), export_sweep_markdown() in exporters.py
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-student-experience/04-CONTEXT.md
@.planning/phases/04-student-experience/04-RESEARCH.md
@.planning/phases/04-student-experience/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 display.py (must exist before this plan runs) -->
From llm_benchmark/display.py:
```python
def render_text_bar_chart(
    rankings: list[tuple[str, float]],
    metric_label: str = "t/s",
) -> str:
    """Return a text-only bar chart suitable for Markdown reports.
    Pure Unicode, no Rich markup, no ANSI codes.
    Includes recommendation line at end."""
```

From llm_benchmark/models.py:
```python
class ModelSummary(BaseModel):
    model: str
    avg_prompt_eval_ts: float
    avg_response_ts: float
    avg_total_ts: float

class ConcurrentBatchResult(BaseModel):
    model: str
    prompt: str
    num_workers: int
    wall_time_s: float
    aggregate_throughput_ts: float
    avg_request_throughput_ts: float

class SweepModelResult(BaseModel):
    model: str
    configs: list[SweepConfigResult]
    best_config: SweepConfigResult | None = None

class SweepConfigResult(BaseModel):
    model: str
    num_ctx: int
    num_gpu: int
    response_ts: float
    total_ts: float

class SystemInfo(BaseModel):
    cpu: str
    ram_gb: float
    gpu: str
    gpu_vram_gb: float | None = None
    os_name: str
    python_version: str
    ollama_version: str
```

Current export_markdown signature (line 211 of exporters.py):
```python
def export_markdown(
    results: list[ModelSummary],
    system_info: SystemInfo | None = None,
    output_dir: str | Path = "results",
) -> Path:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance standard Markdown exporter with rankings</name>
  <files>llm_benchmark/exporters.py</files>
  <action>
Modify `export_markdown()` in exporters.py to add rankings. The enhanced format should be:

1. **Header**: Replace the current simple header with a compact one-line format:
   ```
   # LLM Benchmark Results

   **Generated:** 2026-03-13 14:30:00 | **Models:** 3 | **Mode:** Standard
   ```

2. **System info**: Replace the multi-line system info with a one-liner:
   ```
   **System:** {cpu}, {ram_gb} GB RAM, {gpu}, {os_name}
   ```

3. **Rankings section**: Add BETWEEN system info and the existing Summary table. Lazy import `render_text_bar_chart` from display.py:
   ```python
   from llm_benchmark.display import render_text_bar_chart
   rankings = sorted(
       [(s.model, s.avg_response_ts) for s in results],
       key=lambda x: x[1], reverse=True,
   )
   chart_text = render_text_bar_chart(rankings)
   ```
   Insert the chart text in a Rankings section with `---` separator.

4. Keep existing Summary table and Detailed Results sections unchanged.

5. Add `mode: str = "standard"` parameter to export_markdown for the header mode label.

Also enhance `export_concurrent_markdown()`:
- Same compact header format with **Mode:** Concurrent (N workers)
- One-line system info
- Rankings section using aggregate_throughput_ts: group by model, take max aggregate_throughput_ts per model, sort descending, render text bar chart with metric_label="t/s (aggregate)"

Also enhance `export_sweep_markdown()`:
- Same compact header format with **Mode:** Sweep
- One-line system info
- Rankings section: for each model with a best_config, use (model, best_config.response_ts), sort descending, render text bar chart
- Add per-model best config callout below chart: "Best config for {model}: num_ctx={X}, num_gpu={Y} ({rate:.1f} t/s)"
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && uv run pytest tests/test_exporters.py -x -q</automated>
  </verify>
  <done>All three Markdown exporters produce reports with compact header, one-line system info, Rankings section with text bar chart, and recommendation. Existing exporter tests pass (may need minor updates for changed format).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_exporters.py -x -q` passes
- `uv run pytest tests/ -x -q` passes (full suite)
- Markdown output includes Rankings section with Unicode bar chart
- Reports are pure text (no ANSI codes), pasteable into GitHub/Discord
</verification>

<success_criteria>
- Standard Markdown report has: compact header, one-line system info, Rankings with bar chart + recommendation, Summary table, Detailed Results
- Concurrent Markdown report ranks by aggregate throughput
- Sweep Markdown report includes per-model best config callout
- All text is plain Unicode (no Rich markup or ANSI codes in files)
- Existing tests pass or are updated for new format
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-experience/04-02-SUMMARY.md`
</output>
