---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - llm_benchmark/__init__.py
  - llm_benchmark/config.py
  - llm_benchmark/models.py
  - llm_benchmark/prompts.py
  - tests/test_package.py
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-05]

must_haves:
  truths:
    - "Package can be imported: `import llm_benchmark` succeeds"
    - "pyproject.toml declares Python >=3.12 and all four dependencies"
    - "Pydantic models validate Ollama responses correctly"
    - "Prompt sets (small/medium/large) are accessible from llm_benchmark.prompts"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata, dependencies, build config"
      contains: "requires-python"
    - path: "llm_benchmark/__init__.py"
      provides: "Package init with version"
      contains: "__version__"
    - path: "llm_benchmark/config.py"
      provides: "Shared console, debug flag, constants"
      exports: ["get_console", "is_debug", "set_debug"]
    - path: "llm_benchmark/models.py"
      provides: "Pydantic data models for Ollama responses and benchmark results"
      exports: ["Message", "OllamaResponse", "BenchmarkResult"]
    - path: "llm_benchmark/prompts.py"
      provides: "Prompt sets for benchmarking"
      exports: ["PROMPT_SETS"]
    - path: "tests/test_package.py"
      provides: "Package import and structure validation tests"
      contains: "test_imports"
  key_links:
    - from: "llm_benchmark/models.py"
      to: "pydantic"
      via: "BaseModel inheritance"
      pattern: "class.*BaseModel"
    - from: "llm_benchmark/config.py"
      to: "rich"
      via: "Console singleton"
      pattern: "Console\\(\\)"
---

<objective>
Create the Python package scaffold with pyproject.toml, shared config, Pydantic data models, and prompt sets. This establishes the foundation that all subsequent plans build on.

Purpose: Every other module imports from config.py (console) and models.py (types). These must exist first.
Output: A valid Python package that can be imported, with all data contracts defined.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@benchmark.py
@extended_benchmark.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package scaffold and pyproject.toml</name>
  <files>pyproject.toml, llm_benchmark/__init__.py, llm_benchmark/config.py, llm_benchmark/prompts.py, tests/__init__.py, tests/test_package.py</files>
  <action>
1. Create `llm_benchmark/` directory.

2. Create `pyproject.toml` with hatchling build backend:
   - name: "llm-benchmark", version: "2.0.0"
   - description: "Measure token throughput of LLMs running via Ollama"
   - requires-python = ">= 3.12"
   - dependencies: ollama>=0.6, pydantic>=2.9, rich>=14.0, tenacity>=9.0
   - [project.optional-dependencies] test = ["pytest>=8.0"]
   - [project.scripts] llm-benchmark = "llm_benchmark.cli:main"

3. Create `llm_benchmark/__init__.py`:
   - Only `__version__ = "2.0.0"` -- keep minimal to avoid circular imports (see Pitfall 6 in RESEARCH.md).

4. Create `llm_benchmark/config.py`:
   - Rich Console singleton via `get_console()` function
   - `_debug: bool = False` module-level flag with `is_debug()` / `set_debug(enabled)` accessors
   - Constants: DEFAULT_TIMEOUT = 300 (seconds), DEFAULT_PROMPT_SET = "medium", DEFAULT_RUNS_PER_PROMPT = 2, RAM_SAFETY_MULTIPLIER = 1.2
   - Use the pattern from RESEARCH.md Pattern 3.

5. Create `llm_benchmark/prompts.py`:
   - Migrate `PROMPT_SETS` dict from extended_benchmark.py (lines 27-67) with "small" (3 prompts), "medium" (5 prompts), "large" (7 prompts).
   - Add `get_prompts(set_name: str) -> list[str]` helper that raises ValueError for unknown set names.

6. Create `tests/__init__.py` (empty) and `tests/test_package.py`:
   - `test_imports`: Verify `import llm_benchmark` succeeds and `llm_benchmark.__version__` is a non-empty string (covers STAB-01 import validation).
   - `test_structure`: Verify all expected submodules exist and are importable: `llm_benchmark.config`, `llm_benchmark.models`, `llm_benchmark.prompts` (covers QUAL-02 structure validation). Note: models.py is created in Task 2, so this test should be run after Task 2 completes. For now, test only config and prompts; Task 2 will extend this test to cover models.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && python -c "from llm_benchmark import __version__; from llm_benchmark.config import get_console, is_debug, set_debug; from llm_benchmark.prompts import PROMPT_SETS, get_prompts; assert __version__ == '2.0.0'; assert len(PROMPT_SETS) == 3; assert not is_debug(); print('OK')"</automated>
  </verify>
  <done>pyproject.toml exists with correct metadata. Package imports work. Console singleton, debug flag, prompts all accessible. tests/test_package.py exists with test_imports and test_structure tests.</done>
</task>

<task type="tdd">
  <name>Task 2: Create Pydantic data models</name>
  <files>llm_benchmark/models.py, tests/test_models.py, tests/conftest.py, tests/test_package.py</files>
  <behavior>
    - OllamaResponse validates a dict with all Ollama timing fields (total_duration, load_duration, prompt_eval_count, prompt_eval_duration, eval_count, eval_duration)
    - OllamaResponse accepts prompt_eval_count=-1 without raising, sets it to 0
    - OllamaResponse does NOT print warnings as a side effect (unlike current benchmark.py validator)
    - BenchmarkResult holds model name, prompt, success flag, OllamaResponse data, and a `prompt_cached: bool` flag
    - Message model has role and content fields
  </behavior>
  <action>
1. Create `tests/conftest.py` with a `sample_ollama_response_dict` fixture returning a valid response dict (model, created_at, message, done, total_duration, load_duration, prompt_eval_count, prompt_eval_duration, eval_count, eval_duration -- use realistic nanosecond values).

2. Create `tests/test_models.py` with tests:
   - `test_ollama_response_valid`: Validates sample dict successfully
   - `test_ollama_response_prompt_cached`: prompt_eval_count=-1 sets to 0 and sets a flag (no print side effect)
   - `test_benchmark_result_success`: BenchmarkResult with success=True has response data
   - `test_benchmark_result_failure`: BenchmarkResult with success=False, error message
   - `test_message_model`: Message with role and content

3. Create `llm_benchmark/models.py`:
   - `Message(BaseModel)`: role: str, content: str
   - `OllamaResponse(BaseModel)`: Migrate from benchmark.py but REMOVE the print() side effect from the validator. Instead, set `prompt_eval_count=0` silently. The caller will detect caching by checking original value.
   - Add `prompt_cached: bool = False` field to OllamaResponse, set to True in validator when prompt_eval_count was -1.
   - `BenchmarkResult(BaseModel)`: model: str, prompt: str, success: bool, response: OllamaResponse | None, error: str | None, prompt_cached: bool = False
   - `ModelSummary(BaseModel)`: model: str, results: list[BenchmarkResult], avg_prompt_eval_ts: float, avg_response_ts: float, avg_total_ts: float
   - `SystemInfo(BaseModel)`: cpu: str, ram_gb: float, gpu: str, gpu_vram_gb: float | None, os_name: str, python_version: str, ollama_version: str
   - Use `compute_averages()` as a classmethod or standalone function that does total_tokens/total_time (STAB-04 fix). This replaces the arithmetic-mean-of-rates bug.

4. Update `tests/test_package.py`: Add `llm_benchmark.models` to the `test_structure` submodule import check so it now verifies config, models, and prompts are all importable.

5. Run tests.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && python -m pytest tests/test_models.py tests/test_package.py -x -q</automated>
  </verify>
  <done>All Pydantic models defined with correct validation. OllamaResponse handles prompt caching without side effects. BenchmarkResult captures success/failure. compute_averages uses total_tokens/total_time (STAB-04). tests/test_package.py covers imports and structure (STAB-01, QUAL-02). Tests pass.</done>
</task>

</tasks>

<verification>
- `python -c "import llm_benchmark"` succeeds
- `python -m pytest tests/test_models.py tests/test_package.py -x -q` all green
- pyproject.toml has requires-python >= 3.12
- No print() side effects in model validators
</verification>

<success_criteria>
- Package structure exists and is importable
- All data contracts (models) are defined with tests
- STAB-04 (correct averaging) is implemented and tested
- Config singleton and prompts are ready for downstream plans
- tests/test_package.py verifies imports (STAB-01) and structure (QUAL-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
