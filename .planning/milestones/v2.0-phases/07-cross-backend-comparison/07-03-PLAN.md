---
phase: 07-cross-backend-comparison
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "README has a 'Multi-Backend Setup' section with per-OS install guides for Ollama, llama.cpp, and LM Studio"
    - "README has a 'Cross-Backend Comparison' section with CLI usage and realistic example output"
    - "Example output shows llama.cpp ~1.5x faster than Ollama on Apple Silicon with realistic numbers"
  artifacts:
    - path: "README.md"
      provides: "Multi-backend documentation"
      contains: "Multi-Backend Setup"
  key_links: []
---

<objective>
Update README.md with multi-backend quick start, per-OS setup guides for all 3 backends, and a cross-backend comparison section showing CLI usage and crafted-but-realistic example output.

Purpose: Students need clear documentation to set up multiple backends and use the comparison feature.
Output: Updated README.md
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cross-backend-comparison/07-CONTEXT.md

<interfaces>
<!-- Install instructions from detection.py for accuracy -->
From llm_benchmark/backends/detection.py:
```python
_INSTALL_INSTRUCTIONS = {
    "ollama": {"macos": "brew install ollama", "windows": "winget install Ollama.Ollama", "linux": "curl -fsSL https://ollama.com/install.sh | sh"},
    # llama-cpp and lm-studio also have per-OS instructions
}
```

CLI usage patterns:
```
python -m llm_benchmark run --backend all
python -m llm_benchmark run --backend llama-cpp --model-path path/to/model.gguf
python -m llm_benchmark run --backend lm-studio
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-backend setup and comparison sections to README</name>
  <files>README.md</files>
  <action>
Read the current README.md first. Then add two new sections after the existing "Quick Start" or main usage section:

**Section 1: "Multi-Backend Setup"** (after existing Quick Start)
- Brief intro: "LLM Benchmark supports three local inference backends"
- Table showing: Backend | Default Port | Best For
  - Ollama (11434) - Easiest setup, model management built-in
  - llama.cpp (8080) - Raw performance, direct GGUF support
  - LM Studio (1234) - GUI-based, easy model browsing

- **macOS (Apple Silicon)** subsection:
  ```
  # Ollama
  brew install ollama && ollama serve
  ollama pull llama3.2:1b

  # llama.cpp
  brew install llama.cpp
  # Download a GGUF model from Hugging Face
  llama-server --model path/to/model.gguf

  # LM Studio
  # Download from https://lmstudio.ai
  lms server start
  ```

- **Windows** subsection:
  ```
  # Ollama
  winget install Ollama.Ollama

  # llama.cpp
  winget install llama-cpp

  # LM Studio
  # Download from https://lmstudio.ai
  ```

- **Linux** subsection:
  ```
  # Ollama
  curl -fsSL https://ollama.com/install.sh | sh

  # llama.cpp
  sudo apt install llama.cpp  # or build from source

  # LM Studio
  # Download AppImage from https://lmstudio.ai
  ```

- "Verify installation" one-liner per backend

**Section 2: "Cross-Backend Comparison"**
- Usage:
  ```
  # Compare all detected backends
  python -m llm_benchmark run --backend all

  # Or use the interactive menu (option 5)
  python -m llm_benchmark
  ```

- Crafted-but-realistic example output showing a comparison run with 2 backends (Ollama + llama.cpp) and 2 models. Use realistic Apple Silicon numbers per CONTEXT.md:
  - llama3.2:1b -- Ollama ~45 t/s, llama.cpp ~62 t/s
  - llama3.2:3b -- Ollama ~28 t/s, llama.cpp ~41 t/s
  - Show the matrix table format with winner column
  - Show "Fastest backend: llama.cpp (2/2 models)" summary

- Note: "Results vary by hardware. Run your own comparison to find the fastest backend for your setup."

Keep all existing README content intact. Only ADD new sections. Match the existing README's formatting style (heading levels, code block style, etc.).
  </action>
  <verify>
    <automated>python -c "
readme = open('README.md').read()
assert 'Multi-Backend Setup' in readme, 'Missing Multi-Backend Setup section'
assert 'Cross-Backend Comparison' in readme, 'Missing Cross-Backend Comparison section'
assert 'llama.cpp' in readme, 'Missing llama.cpp references'
assert 'lm-studio' in readme or 'LM Studio' in readme, 'Missing LM Studio references'
assert '--backend all' in readme, 'Missing --backend all usage'
assert 'Fastest backend' in readme, 'Missing fastest backend example'
print('README validation passed')
"</automated>
  </verify>
  <done>README contains Multi-Backend Setup with per-OS guides for all 3 backends, Cross-Backend Comparison section with CLI usage and realistic example output showing llama.cpp ~1.5x faster on Apple Silicon.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README documentation quality</name>
  <files>README.md</files>
  <action>
Human reviews the README changes for accuracy and formatting:
1. Open README.md and scroll to the new "Multi-Backend Setup" section
2. Verify per-OS install commands look correct for macOS, Windows, Linux
3. Scroll to "Cross-Backend Comparison" section
4. Verify example output looks realistic and the formatting is clear
5. Confirm existing README sections are intact and unchanged
  </action>
  <verify>Human approval</verify>
  <done>User approves README documentation or provides feedback for fixes</done>
</task>

</tasks>

<verification>
- README.md contains "Multi-Backend Setup" section with macOS/Windows/Linux guides
- README.md contains "Cross-Backend Comparison" section with `--backend all` usage
- README.md contains realistic example output with llama.cpp ~1.5x faster
- Existing README content preserved
</verification>

<success_criteria>
- Multi-Backend Setup section has one-liner install + verify for each backend per OS
- Cross-Backend Comparison section has CLI usage and crafted example output
- Example shows realistic Apple Silicon numbers (llama.cpp ~62 t/s vs Ollama ~45 t/s)
- README formatting consistent with existing style
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-backend-comparison/07-03-SUMMARY.md`
</output>
