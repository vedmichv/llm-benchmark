---
phase: 07-cross-backend-comparison
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - llm_benchmark/cli.py
  - llm_benchmark/menu.py
  - tests/test_cli.py
  - tests/test_menu.py
autonomous: true
requirements:
  - COMP-01
  - COMP-05

must_haves:
  truths:
    - "`--backend all` is accepted by the CLI parser and triggers comparison mode"
    - "Menu option 5 ('Compare backends') is visible and triggers comparison with medium prompts and 2 runs"
    - "Menu option 5 with only 1 backend shows install hint instead of erroring"
    - "Modes 1-4 behavior is unchanged"
  artifacts:
    - path: "llm_benchmark/cli.py"
      provides: "--backend all handling"
      contains: "all"
    - path: "llm_benchmark/menu.py"
      provides: "Menu option 5"
      contains: "Compare backends"
  key_links:
    - from: "llm_benchmark/cli.py"
      to: "llm_benchmark/comparison.py"
      via: "run_comparison import when --backend all"
      pattern: "from llm_benchmark.comparison import"
    - from: "llm_benchmark/menu.py"
      to: "llm_benchmark/comparison.py"
      via: "_mode_compare returns Namespace with backend='all'"
      pattern: "backend.*all"
---

<objective>
Wire comparison.py into the CLI (--backend all) and interactive menu (option 5). Add "all" to --backend choices in cli.py. Add comparison branch in _handle_run that detects --backend all, calls run_comparison, exports results. Add option 5 to the interactive menu that auto-runs comparison with medium prompts, 2 runs. Update tests.

Purpose: Makes the comparison feature accessible to users via both CLI and interactive menu.
Output: Updated cli.py, menu.py, test_cli.py, test_menu.py
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-backend-comparison/07-CONTEXT.md
@.planning/phases/07-cross-backend-comparison/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (comparison.py) -->
From llm_benchmark/comparison.py (created in Plan 01):
```python
class ComparisonResult(BaseModel):
    backends: list[str]
    models: list[str]
    results: list[BackendModelResult]
    winner_per_model: dict[str, str]
    overall_winner: str
    overall_wins: dict[str, int]

def run_comparison(
    backends: list[BackendStatus],
    prompts: list[str],
    runs_per_prompt: int,
    timeout: int,
    skip_warmup: bool,
    max_retries: int,
    verbose: bool,
    skip_models: list[str],
    skip_checks: bool,
) -> ComparisonResult: ...

def render_comparison_bar_chart(backend_rates: list[tuple[str, float]], model_name: str) -> None: ...
def render_comparison_matrix(results: dict[str, list[ModelSummary]]) -> None: ...
def export_comparison_json(comparison: ComparisonResult, system_info: SystemInfo, output_dir: str = "results") -> Path: ...
def export_comparison_markdown(comparison: ComparisonResult, system_info: SystemInfo, output_dir: str = "results") -> Path: ...
```

From llm_benchmark/cli.py (current):
```python
def _build_parser() -> argparse.ArgumentParser: ...  # --backend choices: ["ollama", "llama-cpp", "lm-studio"]
def _handle_run(args: argparse.Namespace) -> int: ...  # Dispatches to sweep/concurrent/standard modes
```

From llm_benchmark/menu.py (current):
```python
def _prompt_choice(prompt_text: str, valid: set[str]) -> str: ...
def _build_namespace(...) -> argparse.Namespace: ...
def run_interactive_menu(backend, models: list) -> argparse.Namespace: ...  # Options 1-4
def select_backend_interactive() -> tuple[str, int | None, str | None]: ...
```

From llm_benchmark/backends/detection.py:
```python
def detect_backends(overrides: dict[str, int] | None = None) -> list[BackendStatus]: ...
def get_install_instructions(name: str) -> dict[str, str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --backend all to CLI and comparison handler in _handle_run</name>
  <files>llm_benchmark/cli.py, tests/test_cli.py</files>
  <behavior>
    - Test: CLI parser accepts --backend all without error
    - Test: --backend all triggers comparison branch (not sweep/concurrent/standard)
    - Test: --backend all with only 1 detected backend still works (falls back gracefully)
  </behavior>
  <action>
In cli.py _build_parser(): Add "all" to --backend choices list: `choices=["ollama", "llama-cpp", "lm-studio", "all"]`. Update help text: `help="Backend to use (default: ollama, 'all' for cross-backend comparison)"`.

In cli.py _handle_run(): Add a comparison branch at the TOP of the function, before the existing backend creation. Check `if args.backend == "all":` and:
1. Lazy-import `detect_backends` from `llm_benchmark.backends.detection`
2. Lazy-import `run_comparison`, `export_comparison_json`, `export_comparison_markdown` from `llm_benchmark.comparison`
3. Call `detect_backends()` to get running backends
4. Filter to only `running=True` backends
5. If 0 running backends: error message and return 1
6. Call `run_comparison(backends=running, prompts=get_prompts(args.prompt_set), runs_per_prompt=args.runs_per_prompt, timeout=args.timeout, skip_warmup=args.skip_warmup, max_retries=args.max_retries, verbose=args.verbose, skip_models=args.skip_models, skip_checks=args.skip_checks)`
7. Get system_info via get_system_info() (use first running backend for system info)
8. Call export_comparison_json and export_comparison_markdown
9. Print export paths
10. Return 0

In tests/test_cli.py: Add test that parses `["run", "--backend", "all"]` and verifies `args.backend == "all"`. Add test mocking detect_backends and run_comparison to verify comparison branch is triggered.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -x -v</automated>
  </verify>
  <done>--backend all accepted by parser. Comparison branch in _handle_run imports and calls run_comparison. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add menu option 5 ("Compare backends") to interactive menu</name>
  <files>llm_benchmark/menu.py, tests/test_menu.py</files>
  <behavior>
    - Test: Menu displays option 5 "Compare backends" in output
    - Test: Choosing "5" returns Namespace with backend="all", prompt_set="medium", runs_per_prompt=2
    - Test: _mode_compare with only 1 detected backend shows install hint message
    - Test: Modes 1-4 still work identically (regression)
  </behavior>
  <action>
In menu.py run_interactive_menu():
1. Add `console.print("  5. Compare backends")` after option 4
2. Change `_prompt_choice("Select mode [1-5]: ", {"1", "2", "3", "4", "5"})`
3. Add `if choice == "5": return _mode_compare(backend)` before the final return

Create `_mode_compare(backend) -> argparse.Namespace` in menu.py:
1. Lazy-import `detect_backends` from `llm_benchmark.backends.detection`
2. Lazy-import `get_install_instructions` from `llm_benchmark.backends.detection`
3. Call `detect_backends()` and filter to `running=True`
4. If only 1 running backend:
   - Print "[yellow]Only 1 backend detected. Install another backend to compare:[/yellow]"
   - Print install hints for backends not currently installed using `get_install_instructions()`
   - Print hint for each missing backend (macOS instructions since that's what most students use, but show the function's output for current platform)
   - Still return a Namespace with `backend="all"` -- let run_comparison handle the single-backend fallback
5. If multiple running: print "[bold]Comparing {N} backends: {names}[/bold]"
6. Return `_build_namespace(backend="all", prompt_set="medium", runs_per_prompt=2, skip_warmup=False, max_retries=3, timeout=120, verbose=False, skip_models=[], skip_checks=False, sweep=False, concurrent=0, prompts=None, model_path=None, sort_by=None, top_n=None, port=None)`

In tests/test_menu.py: Add tests for option 5 selection, _mode_compare with mocked detect_backends returning 1 backend and 2 backends. Verify existing tests for modes 1-4 still pass (run full test_menu.py).
  </action>
  <verify>
    <automated>uv run pytest tests/test_menu.py -x -v</automated>
  </verify>
  <done>Menu shows option 5. Choosing 5 returns correct Namespace. Single-backend case shows install hints. Modes 1-4 unchanged. All menu tests pass. Full suite green: uv run pytest tests/ -x -q</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_cli.py tests/test_menu.py -x -v` -- all CLI and menu tests pass
- `uv run pytest tests/ -x -q` -- full suite still green
- `python -c "from llm_benchmark.cli import _build_parser; p = _build_parser(); a = p.parse_args(['run', '--backend', 'all']); assert a.backend == 'all'"` -- CLI accepts --backend all
</verification>

<success_criteria>
- `--backend all` is a valid CLI option that triggers comparison mode
- Menu option 5 is visible and produces correct Namespace for comparison
- Single-backend fallback handled gracefully in both CLI and menu paths
- Existing modes 1-4 and backend choices unaffected
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-backend-comparison/07-02-SUMMARY.md`
</output>
