---
phase: 07-cross-backend-comparison
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - llm_benchmark/cli.py
  - tests/test_cli.py
autonomous: true
gap_closure: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Running --backend all with a single model displays a horizontal bar chart in the terminal with backend names and star on fastest"
    - "Running --backend all with multiple models displays a Rich matrix table in the terminal with winner per row and overall fastest recommendation"
  artifacts:
    - path: "llm_benchmark/cli.py"
      provides: "Display calls wired into --backend all branch"
      contains: "render_comparison"
    - path: "tests/test_cli.py"
      provides: "Test verifying display functions are called"
      contains: "render_comparison"
  key_links:
    - from: "llm_benchmark/cli.py"
      to: "llm_benchmark/comparison.py"
      via: "render_comparison_bar_chart and render_comparison_matrix calls"
      pattern: "render_comparison_(bar_chart|matrix)"
---

<objective>
Wire comparison display functions into the CLI execution path so students see bar chart / matrix output in the terminal when running `--backend all`.

Purpose: The display functions exist and are tested but are never called from the CLI. This is the only verification gap in Phase 7 -- fixing it completes the phase.
Output: Updated cli.py with display calls, updated test coverage.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-backend-comparison/07-VERIFICATION.md

<interfaces>
<!-- From llm_benchmark/comparison.py — display function signatures -->

```python
def render_comparison_bar_chart(
    backend_rates: list[tuple[str, float]],
    model_name: str,
) -> None:
    """Bar chart with backends as entries for a single model."""

def render_comparison_matrix(
    results: dict[str, list],  # backend_name -> list[ModelSummary]
) -> None:
    """Rich Table showing models as rows, backends as columns, with winner per row."""
```

<!-- From llm_benchmark/comparison.py — ComparisonResult model -->

```python
class ComparisonResult(BaseModel):
    backends: list[str]
    models: list[str]
    results: list[BackendModelResult]  # flat list, each has .backend, .model, .avg_response_ts
    winner_per_model: dict[str, str]
    overall_winner: str
    overall_wins: dict[str, int]
```

<!-- From llm_benchmark/comparison.py — run_comparison returns ComparisonResult -->
<!-- all_backend_results dict (backend_name -> list[ModelSummary]) is internal to run_comparison -->
<!-- The CLI only has access to the ComparisonResult object after the call -->

<!-- Note: render_comparison_matrix takes dict[str, list] (backend -> ModelSummary list) -->
<!-- but cli.py only has ComparisonResult. Two options: -->
<!-- 1. Reconstruct the dict from ComparisonResult.results (BackendModelResult has .backend, .model, .avg_response_ts) -->
<!-- 2. Call render_comparison_bar_chart for single model (uses list[tuple[str, float]]) -->
<!-- For matrix: can build a mock-like dict from BackendModelResult objects since render_comparison_matrix only reads .model and .avg_response_ts -->

<!-- From cli.py line 251-261: the comparison branch structure -->
```python
# Current code in _handle_run (lines 251-271):
comparison = run_comparison(
    backends=running,
    prompts=prompts,
    runs_per_prompt=args.runs_per_prompt,
    timeout=args.timeout,
    skip_warmup=args.skip_warmup,
    max_retries=args.max_retries,
    verbose=args.verbose,
    skip_models=args.skip_models,
    skip_checks=args.skip_checks,
)

# Export results (lines 263-271)
first_backend = create_backend(running[0].name)
system_info = get_system_info(backend=first_backend)
json_path = export_comparison_json(comparison, system_info)
md_path = export_comparison_markdown(comparison, system_info)
console.print("[bold]Comparison results saved:[/bold]")
console.print(f"  JSON: {json_path}")
console.print(f"  MD:   {md_path}")
return 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire display functions into CLI --backend all branch</name>
  <files>llm_benchmark/cli.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: When --backend all returns a ComparisonResult with 1 model, render_comparison_bar_chart is called with correct (backend, rate) tuples and model name
    - Test 2: When --backend all returns a ComparisonResult with 2+ models, render_comparison_matrix is called with a dict mapping backend names to result lists
    - Test 3: Display functions are called BEFORE export functions (so student sees terminal output before file paths)
  </behavior>
  <action>
In `llm_benchmark/cli.py`, in the `_handle_run` function's `--backend all` branch (after line 261, before the export block starting at line 263):

1. Add `render_comparison_bar_chart` and `render_comparison_matrix` to the lazy import block at lines 235-239.

2. Insert display logic between `run_comparison()` and the export block:
   - If `len(comparison.models) == 1`: build `backend_rates` as `[(r.backend, r.avg_response_ts) for r in comparison.results]` and call `render_comparison_bar_chart(backend_rates, comparison.models[0])`
   - Else (multiple models): build a `results_by_backend` dict mapping each backend name to its list of BackendModelResult objects from `comparison.results`, then call `render_comparison_matrix(results_by_backend)`. Note: `render_comparison_matrix` reads `.model` and `.avg_response_ts` from items, and `BackendModelResult` has both fields, so the types are compatible.

In `tests/test_cli.py`, update the `TestBackendAll` class:

3. In the existing `test_backend_all_triggers_comparison_branch` test, add the display function mocks to the patch context and assert they are called. Set `mock_comparison.models = ["llama3.2:1b"]` and `mock_comparison.results = [MagicMock(backend="ollama", avg_response_ts=42.0)]` to trigger the single-model path. Assert `render_comparison_bar_chart` was called once.

4. Add a new test `test_backend_all_multi_model_calls_matrix` that sets `mock_comparison.models = ["model-a", "model-b"]` with matching results, and asserts `render_comparison_matrix` is called once and `render_comparison_bar_chart` is NOT called.

Use `from llm_benchmark.config import get_console` for any console usage per project convention. Do NOT use print().
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && uv run pytest tests/test_cli.py::TestBackendAll -x -v</automated>
  </verify>
  <done>
    - render_comparison_bar_chart called for single-model --backend all runs
    - render_comparison_matrix called for multi-model --backend all runs
    - Display output appears before "Comparison results saved" file paths
    - All TestBackendAll tests pass including the new ones
    - Full test suite still passes: uv run pytest tests/ -x -q
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_cli.py::TestBackendAll -x -v` -- all comparison CLI tests pass
2. `uv run pytest tests/ -x -q` -- full suite green, no regressions
3. Grep confirms wiring: `grep -n "render_comparison" llm_benchmark/cli.py` shows both function calls
</verification>

<success_criteria>
- render_comparison_bar_chart is called in cli.py when --backend all produces 1 model
- render_comparison_matrix is called in cli.py when --backend all produces 2+ models
- Display calls happen before export calls in the code path
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-backend-comparison/07-04-SUMMARY.md`
</output>
