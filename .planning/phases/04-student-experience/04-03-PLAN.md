---
phase: 04-student-experience
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - tests/test_menu.py
  - tests/test_display.py
  - tests/test_exporters.py
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "Running `uv run pytest tests/ --cov=llm_benchmark --cov-fail-under=60` passes"
    - "test_menu.py tests all 4 modes with mocked input() and verifies returned Namespace"
    - "test_display.py tests bar chart output and text chart string generation"
    - "CI workflow runs ruff lint + py_compile + pytest on push to main/master and PRs"
    - "ruff check passes on all source files"
  artifacts:
    - path: "tests/test_menu.py"
      provides: "Unit tests for interactive menu module"
      min_lines: 60
    - path: "tests/test_display.py"
      provides: "Unit tests for bar chart and recommendation display"
      min_lines: 40
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      contains: "ruff check"
    - path: "pyproject.toml"
      provides: "Dev dependencies (ruff, pytest-cov) and ruff config"
      contains: "ruff"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/"
      via: "uv run pytest tests/ -x -q"
      pattern: "pytest"
    - from: "pyproject.toml"
      to: ".github/workflows/ci.yml"
      via: "ruff config used by CI lint step"
      pattern: "tool\\.ruff"
---

<objective>
Add unit tests for new modules (menu, display, enhanced exporters), configure ruff linting, and create GitHub Actions CI workflow.

Purpose: Ensure >60% test coverage with mocked Ollama, and automate quality checks so students and contributors get fast feedback on push/PR.
Output: test_menu.py, test_display.py, updated test_exporters.py, pyproject.toml with ruff+pytest-cov, .github/workflows/ci.yml
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-student-experience/04-CONTEXT.md
@.planning/phases/04-student-experience/04-RESEARCH.md
@.planning/phases/04-student-experience/04-01-SUMMARY.md
@.planning/phases/04-student-experience/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From llm_benchmark/menu.py:
```python
def run_interactive_menu(models: list) -> argparse.Namespace:
    """Present interactive menu, return Namespace matching _handle_run expectations."""
```

From llm_benchmark/display.py:
```python
BAR_FULL = "\u2588"
BAR_EMPTY = "\u2591"
BAR_WIDTH = 30

def render_bar_chart(rankings: list[tuple[str, float]], metric_label: str = "t/s") -> None:
    """Print ranked bar chart to console."""

def render_text_bar_chart(rankings: list[tuple[str, float]], metric_label: str = "t/s") -> str:
    """Return text-only bar chart for Markdown."""
```

<!-- From Plan 02 -->
From llm_benchmark/exporters.py:
```python
def export_markdown(results, system_info=None, output_dir="results", mode="standard") -> Path:
    # Enhanced with Rankings section
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ruff, pytest-cov to pyproject.toml and fix lint issues</name>
  <files>pyproject.toml</files>
  <action>
1. Update pyproject.toml dev dependencies. Merge into the existing `[dependency-groups] dev` section:
   ```toml
   [dependency-groups]
   dev = [
       "pytest>=8.0",
       "pytest-asyncio>=1.3.0",
       "pytest-cov>=7.0",
       "ruff>=0.9",
   ]
   ```
   Also move pytest from `[project.optional-dependencies] test` into dev group (consolidate). Remove the `[project.optional-dependencies]` test section if it only had pytest.

2. Add ruff configuration:
   ```toml
   [tool.ruff]
   target-version = "py312"
   line-length = 88

   [tool.ruff.lint]
   select = ["E", "F", "I", "UP", "B", "SIM"]
   ```

3. Add pytest configuration:
   ```toml
   [tool.pytest.ini_options]
   testpaths = ["tests"]
   ```

4. Run `uv sync` to install new dev deps, then run `uv run ruff check llm_benchmark/ tests/` and fix any lint issues found. Common fixes: import sorting, unused imports, f-string issues. Do NOT rewrite working code -- only fix what ruff flags.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && uv run ruff check llm_benchmark/ tests/</automated>
  </verify>
  <done>ruff check passes cleanly on all source and test files, pytest-cov available for coverage measurement</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for menu, display, enhanced exporters, and CI workflow</name>
  <files>tests/test_menu.py, tests/test_display.py, tests/test_exporters.py, .github/workflows/ci.yml</files>
  <action>
1. Create `tests/test_menu.py` with mocked input() tests:
   - `test_quick_test_mode`: Mock `input` returning "1", mock `format_system_summary`. Create mock model objects (MagicMock with .model="llama3.2:1b", .size=1_000_000_000). Verify returned Namespace has runs_per_prompt=1, skip_warmup=True, and skip_models excludes all but smallest.
   - `test_standard_mode`: Mock input returning "2". Verify prompt_set="medium", runs_per_prompt=2.
   - `test_full_mode`: Mock input returning "3". Verify prompt_set="large", runs_per_prompt=3.
   - `test_custom_mode`: Mock input with side_effect for multiple inputs (mode "4", prompt set "2", runs "3", model skip ""). Verify returned values.
   - `test_invalid_input_reprompts`: Mock input with side_effect ["x", "abc", "1"]. Verify it calls input 3 times.
   - `test_smallest_model_selection`: Create 3 mock models with different sizes, verify quick test picks smallest by .size.
   - Mock `get_console()` to capture output or use `io.StringIO`. Mock `format_system_summary` to return a simple string.

2. Create `tests/test_display.py`:
   - `test_render_bar_chart_output`: Use `capsys` or mock console to capture output. Pass rankings [(model_a, 40.0), (model_b, 20.0)]. Verify output contains both model names, BAR_FULL chars, rates, and "Best for your setup".
   - `test_render_bar_chart_empty`: Pass empty list, verify no output/no crash.
   - `test_render_text_bar_chart_returns_string`: Pass rankings, verify return type is str, contains model names, rates, "Best for your setup", no Rich markup tags.
   - `test_render_text_bar_chart_sorted`: Verify fastest model appears first in output.
   - `test_bar_proportions`: With rankings [(a, 100), (b, 50)], verify b's bar is roughly half the width of a's.

3. Update `tests/test_exporters.py` -- ADD new tests (do not remove existing):
   - `test_export_markdown_has_rankings`: Call export_markdown with sample ModelSummary list and SystemInfo, read the output file, verify it contains "## Rankings", bar chart characters, "Best for your setup".
   - `test_export_markdown_compact_header`: Verify output contains "**Mode:** Standard" and model count.
   - `test_export_markdown_one_line_system_info`: Verify output has "**System:**" one-liner (not multi-line bullet list).

4. Create `.github/workflows/ci.yml`:
   ```yaml
   name: CI

   on:
     push:
       branches: [main, master]
     pull_request:
       branches: [main, master]

   jobs:
     lint-test:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4

         - name: Set up Python 3.12
           uses: actions/setup-python@v5
           with:
             python-version: "3.12"

         - name: Install uv
           uses: astral-sh/setup-uv@v5

         - name: Install dependencies
           run: uv sync --all-extras --dev

         - name: Lint with ruff
           run: uv run ruff check llm_benchmark/ tests/

         - name: Compile check
           run: |
             python -m py_compile llm_benchmark/__init__.py
             python -m py_compile llm_benchmark/cli.py
             python -m py_compile llm_benchmark/runner.py
             python -m py_compile llm_benchmark/models.py
             python -m py_compile llm_benchmark/exporters.py
             python -m py_compile llm_benchmark/config.py
             python -m py_compile llm_benchmark/menu.py
             python -m py_compile llm_benchmark/display.py

         - name: Run tests
           run: uv run pytest tests/ -x -q
   ```

5. Run full test suite with coverage: `uv run pytest tests/ -x -q --cov=llm_benchmark --cov-fail-under=60`. If coverage is below 60%, add targeted tests for the lowest-coverage modules (likely exporters at 36% -- add tests for export_json, export_csv basic behavior).
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && uv run pytest tests/ -x -q --cov=llm_benchmark --cov-fail-under=60</automated>
  </verify>
  <done>All tests pass, coverage >= 60%, ruff lint passes, CI workflow file exists at .github/workflows/ci.yml with lint+compile+test steps</done>
</task>

</tasks>

<verification>
- `uv run ruff check llm_benchmark/ tests/` passes cleanly
- `uv run pytest tests/ -x -q --cov=llm_benchmark --cov-fail-under=60` passes
- `.github/workflows/ci.yml` exists with ruff, py_compile, and pytest steps
- `tests/test_menu.py` covers all 4 modes + invalid input + smallest model selection
- `tests/test_display.py` covers bar chart rendering + text chart + empty input
</verification>

<success_criteria>
- Test coverage >= 60% across llm_benchmark package
- ruff lint passes on all source and test files
- CI workflow targets Python 3.12, runs on push to main/master and PRs
- New test files cover menu.py, display.py, and enhanced exporters
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-experience/04-03-SUMMARY.md`
</output>
