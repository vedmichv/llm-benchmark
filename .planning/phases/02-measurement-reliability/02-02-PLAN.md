---
phase: 02-measurement-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - llm_benchmark/runner.py
  - llm_benchmark/exporters.py
  - llm_benchmark/models.py
  - tests/test_runner.py
  - tests/test_exporters.py
  - results/.gitignore
autonomous: true
requirements:
  - BENCH-07
  - UX-04

must_haves:
  truths:
    - "Cached runs show [cached] tag in terminal output"
    - "First cache detection in a session shows one-liner explanation"
    - "When ALL runs for a model are cached, a warning about unavailable prompt eval metrics is shown"
    - "CSV export includes a Cached column"
    - "Markdown export includes [cached] indicator on affected runs"
    - "results/ directory has .gitignore preventing accidental commits"
    - "Duplicate compute_averages in models.py is removed (runner.py is canonical)"
  artifacts:
    - path: "llm_benchmark/runner.py"
      provides: "Cache visibility in benchmark_model terminal output"
      contains: "cached"
    - path: "llm_benchmark/exporters.py"
      provides: "Cached column in CSV, [cached] indicator in Markdown"
      contains: "Cached"
    - path: "results/.gitignore"
      provides: "Git ignore for benchmark result files"
      contains: "*.json"
    - path: "tests/test_exporters.py"
      provides: "Tests for cache columns in CSV/Markdown exports"
      contains: "TestCsvCacheColumn"
    - path: "tests/test_runner.py"
      provides: "TestCacheVisibility test class"
      contains: "TestCacheVisibility"
  key_links:
    - from: "llm_benchmark/runner.py"
      to: "llm_benchmark/models.py"
      via: "BenchmarkResult.prompt_cached drives [cached] display"
      pattern: "result\\.prompt_cached"
    - from: "llm_benchmark/exporters.py"
      to: "llm_benchmark/models.py"
      via: "CSV/Markdown read prompt_cached from BenchmarkResult"
      pattern: "prompt_cached"
---

<objective>
Make prompt caching visible across all output formats and organize results directory.

Purpose: Students understand why prompt eval shows 0 instead of being silently confused (BENCH-07), and benchmark outputs go to results/ with .gitignore (UX-04).
Output: [cached] terminal tags, cache explanation, CSV/Markdown cache indicators, results/.gitignore, cleaned up duplicate code.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-measurement-reliability/02-CONTEXT.md
@.planning/phases/02-measurement-reliability/02-RESEARCH.md
@.planning/phases/02-measurement-reliability/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From llm_benchmark/runner.py (after Plan 01):
```python
def warmup_model(model_name: str, timeout: int = DEFAULT_TIMEOUT) -> bool: ...
def run_single_benchmark(model_name, prompt, verbose=False, timeout=DEFAULT_TIMEOUT, max_retries=DEFAULT_MAX_RETRIES) -> BenchmarkResult: ...
def benchmark_model(model_name, prompts, verbose=False, timeout=DEFAULT_TIMEOUT, runs_per_prompt=1, skip_warmup=False, max_retries=DEFAULT_MAX_RETRIES) -> ModelSummary: ...
def compute_averages(results: list[BenchmarkResult]) -> dict: ...
```

From llm_benchmark/models.py:
```python
class BenchmarkResult(BaseModel):
    model: str; prompt: str; success: bool
    response: OllamaResponse | None = None
    error: str | None = None; prompt_cached: bool = False

# NOTE: models.py also has a duplicate compute_averages() at line 86-125
# that does NOT exclude cached results. This must be removed.
```

From llm_benchmark/exporters.py:
```python
def _result_to_dict(result) -> dict: ...  # Already includes prompt_cached
def export_csv(results, system_info=None, output_dir="results") -> Path: ...
def export_markdown(results, system_info=None, output_dir="results") -> Path: ...
# CSV headers currently: Model, Prompt, Success, Prompt Tokens, Response Tokens,
#   Prompt Eval (t/s), Response (t/s), Total Time (s), Error
# Missing: Cached column
# Markdown detailed results: no [cached] indicator
```

From tests/conftest.py:
```python
@pytest.fixture
def sample_ollama_response_dict(): ...  # prompt_eval_count=15
@pytest.fixture
def cached_ollama_response_dict(): ...  # prompt_eval_count=-1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cache visibility to terminal output and clean up duplicate code</name>
  <files>llm_benchmark/runner.py, llm_benchmark/models.py, tests/test_runner.py</files>
  <behavior>
    - TestCacheVisibility: when result.prompt_cached is True, "[cached]" appears in console output
    - TestCacheVisibility: first cached result in session triggers one-liner explanation about prompt caching
    - TestCacheVisibility: second cached result does NOT repeat the explanation
    - TestCacheVisibility: when ALL results for a model are cached, warning about unavailable prompt eval metrics is shown
    - TestCacheVisibility: non-cached results do NOT show [cached] tag
    - TestComputeAveragesRunner: existing tests still pass after models.py cleanup
  </behavior>
  <action>
1. Remove duplicate `compute_averages()` from models.py (lines 86-125). The version in runner.py is canonical because it correctly excludes cached results from prompt_eval calculations. Any code importing from models.py should import from runner.py instead. Check if anything imports compute_averages from models -- if so, update the import.

2. In runner.py, modify `benchmark_model()` to show cache indicators:
   - Add a local variable `_cache_explanation_shown = False` at the top of the function
   - After each successful result is printed (the green tokens line), check if `result.prompt_cached`:
     - If cached: append " [dim][cached][/dim]" to the output line
     - If cached AND not _cache_explanation_shown: print one-liner explanation:
       `"    [dim italic]Prompt caching: Ollama reused the prompt from memory, so prompt eval time is 0.[/dim italic]"`
       Set _cache_explanation_shown = True
   - After all prompts are processed for a model, check if ALL successful results have prompt_cached=True:
     - If yes, print: `"  [yellow]All runs cached -- prompt eval metrics unavailable for this model[/yellow]"`

3. Write tests in tests/test_runner.py:
   - class TestCacheVisibility: mock run_single_benchmark to return cached/non-cached BenchmarkResults, call benchmark_model, capture console output (use io.StringIO with Rich Console), verify [cached] tag, explanation, and all-cached warning appear/don't appear as expected
  </action>
  <verify>
    <automated>uv run pytest tests/test_runner.py -x -q</automated>
  </verify>
  <done>[cached] tag shown on cached runs, one-time explanation shown on first cache detection, all-cached warning shown when applicable, duplicate compute_averages removed from models.py, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cache indicators to CSV/Markdown exports and create results/.gitignore</name>
  <files>llm_benchmark/exporters.py, tests/test_exporters.py, results/.gitignore</files>
  <behavior>
    - TestCsvCacheColumn: CSV output includes "Cached" column header
    - TestCsvCacheColumn: cached run rows show "Yes" in Cached column
    - TestCsvCacheColumn: non-cached run rows show "No" in Cached column
    - TestMarkdownCacheIndicator: Markdown detailed results show "[cached]" for cached runs
    - TestMarkdownCacheIndicator: non-cached runs do not show "[cached]"
    - TestResultsGitignore: results/.gitignore exists and ignores *.json, *.csv, *.md but not itself
    - TestExporterOutputDir: export functions default to "results/" directory
  </behavior>
  <action>
1. In exporters.py `export_csv()`:
   - Add "Cached" column to headers (after "Success", before "Prompt Tokens")
   - In the row writer for successful runs: add `"Yes" if run.prompt_cached else "No"`
   - In the row writer for failed runs: add `""` for the Cached column

2. In exporters.py `export_markdown()`:
   - In detailed results section, when listing each run: if `run.prompt_cached`, append " [cached]" after the status text
   - Example: `1. **OK** [cached] \`prompt text...\``

3. Create `results/.gitignore` with contents:
   ```
   # Benchmark result files -- do not commit
   *.json
   *.csv
   *.md
   !.gitignore
   ```

4. In exporters.py, add a helper to ensure results/.gitignore exists:
   - In `_ensure_dir()`, after creating the directory, check if `output_dir` is "results" (or path ends with "results") and if `.gitignore` doesn't exist there yet, create it with the patterns above
   - This auto-creates the .gitignore on first export run

5. Create tests/test_exporters.py:
   - Create test fixtures: build ModelSummary objects with mixed cached/non-cached BenchmarkResults
   - class TestCsvCacheColumn: call export_csv with test data to a tmp_path, read the CSV, verify "Cached" header exists and values are "Yes"/"No" correctly
   - class TestMarkdownCacheIndicator: call export_markdown with test data to a tmp_path, read the Markdown, verify "[cached]" appears for cached runs only
   - class TestResultsGitignore: call export_json (or export_csv) with output_dir as a "results" subdir of tmp_path, verify .gitignore was created with expected content
   - class TestExporterOutputDir: verify export functions create files under the specified output directory
  </action>
  <verify>
    <automated>uv run pytest tests/test_exporters.py -x -q</automated>
  </verify>
  <done>CSV has Cached column, Markdown shows [cached] on cached runs, results/.gitignore exists and ignores benchmark files, _ensure_dir auto-creates .gitignore for results/ dir, all exporter tests pass</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
uv run pytest tests/ -x -q

# Verify no duplicate compute_averages in models.py
python -c "
import inspect, llm_benchmark.models as m
assert not hasattr(m, 'compute_averages') or 'compute_averages' not in [name for name, _ in inspect.getmembers(m, inspect.isfunction) if name == 'compute_averages'], 'Duplicate still exists'
print('No duplicate compute_averages in models.py')
"

# Verify results/.gitignore exists
cat results/.gitignore

# Verify CSV has Cached column
python -c "from llm_benchmark.exporters import export_csv; print('export_csv importable')"
```
</verification>

<success_criteria>
- [cached] tag visible in terminal for cached benchmark runs
- One-time educational explanation shown on first cache detection per session
- All-cached warning shown when every run for a model is cached
- CSV export has "Cached" column with Yes/No values
- Markdown export shows [cached] on affected runs
- results/.gitignore prevents accidental commits of benchmark data
- Duplicate compute_averages removed from models.py
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-measurement-reliability/02-02-SUMMARY.md`
</output>
