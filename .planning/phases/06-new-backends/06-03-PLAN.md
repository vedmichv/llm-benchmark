---
phase: 06-new-backends
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - llm_benchmark/backends/__init__.py
  - llm_benchmark/preflight.py
  - llm_benchmark/cli.py
  - tests/test_preflight.py
  - tests/test_cli.py
autonomous: true
requirements: [BEND-05, CLI-01, PLAT-01]

must_haves:
  truths:
    - "create_backend('llama-cpp') returns LlamaCppBackend, create_backend('lm-studio') returns LMStudioBackend"
    - "`--backend llama-cpp` flag is accepted by CLI and creates correct backend"
    - "Default `--backend` is 'ollama' with no behavior change for existing users"
    - "Preflight checks are backend-aware: install check, connectivity, and model availability work for any backend"
    - "Preflight runs ONLY for the selected backend, not all detected backends"
    - "Optional `--port` and `--model-path` flags work for llama-cpp backend"
  artifacts:
    - path: "llm_benchmark/backends/__init__.py"
      provides: "Extended create_backend() factory supporting all three backends"
      contains: "llama-cpp"
    - path: "llm_benchmark/preflight.py"
      provides: "Backend-generic preflight checks"
      contains: "def run_preflight_checks"
    - path: "llm_benchmark/cli.py"
      provides: "CLI with --backend, --port, --model-path flags"
      contains: "--backend"
  key_links:
    - from: "llm_benchmark/cli.py"
      to: "llm_benchmark/backends/__init__.py"
      via: "create_backend(args.backend, port=args.port)"
      pattern: "create_backend.*args\\.backend"
    - from: "llm_benchmark/preflight.py"
      to: "llm_benchmark/backends/detection.py"
      via: "uses detect_backends and get_install_instructions"
      pattern: "from llm_benchmark\\.backends\\.detection import"
    - from: "llm_benchmark/cli.py"
      to: "llm_benchmark/preflight.py"
      via: "run_preflight_checks(backend=backend)"
      pattern: "run_preflight_checks"
---

<objective>
Wire the new backends into the CLI and preflight system: extend create_backend() factory, add --backend/--port/--model-path CLI flags, and generalize preflight checks to work with any backend.

Purpose: This is the integration layer that makes new backends usable from the command line. Without this, the backend classes from Plan 01 are isolated.
Output: Working `python -m llm_benchmark run --backend llama-cpp --model-path /path/to/model.gguf` command path.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-new-backends/06-CONTEXT.md
@.planning/phases/06-new-backends/06-RESEARCH.md
@.planning/phases/06-new-backends/06-01-SUMMARY.md
@.planning/phases/06-new-backends/06-02-SUMMARY.md

<interfaces>
From llm_benchmark/backends/__init__.py (current):
```python
def create_backend(name: str = "ollama") -> Backend:
    if name == "ollama":
        from llm_benchmark.backends.ollama import OllamaBackend
        return OllamaBackend()
    raise ValueError(f"Unknown backend: {name!r}. Supported backends: 'ollama'")
```

From llm_benchmark/cli.py (current _handle_run):
```python
backend = create_backend()  # Hardcoded, no args
models = run_preflight_checks(backend=backend, skip_models=args.skip_models, skip_checks=args.skip_checks)
```

From llm_benchmark/preflight.py (current):
```python
def check_ollama_installed() -> bool:  # Ollama-specific, needs generalization
def check_backend_connectivity(backend: Backend) -> bool:  # Already backend-generic
def check_available_models(backend: Backend, skip_models=None) -> list[dict]:  # Already backend-generic
def run_preflight_checks(backend=None, skip_models=None, skip_checks=False) -> list[dict]:
    # Step 0: check_ollama_installed() -- needs to become backend-aware
    # Step 1: check_backend_connectivity(backend) -- already generic
    # Step 2: check_available_models(backend) -- already generic
    # Step 3: check_ram_for_models(models) -- already generic
```

From llm_benchmark/backends/detection.py (created in Plan 02):
```python
@dataclass
class BackendStatus:
    name: str
    installed: bool
    running: bool
    binary_path: str | None
    port: int

def detect_backends(port_overrides=None) -> list[BackendStatus]: ...
def auto_start_backend(backend_name, *, port=None, model_path=None, host="127.0.0.1", timeout=30) -> subprocess.Popen: ...
def get_install_instructions(backend_name: str, os_name: str | None = None) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend create_backend() and add CLI flags</name>
  <files>llm_benchmark/backends/__init__.py, llm_benchmark/cli.py, tests/test_cli.py</files>
  <action>
    **Update create_backend() in backends/__init__.py:**
    - Accept optional `host` and `port` parameters: `create_backend(name="ollama", *, host=None, port=None) -> Backend`
    - Add "llama-cpp" branch: lazy import LlamaCppBackend, pass host/port if provided
    - Add "lm-studio" branch: lazy import LMStudioBackend, pass host/port if provided
    - Update error message to list all supported backends: "'ollama', 'llama-cpp', 'lm-studio'"
    - Keep lazy imports (inside if branches) to avoid ImportError for users without httpx

    **Update _build_parser() in cli.py:**
    - Add `--backend` argument to run_parser: choices=["ollama", "llama-cpp", "lm-studio"], default="ollama", help text mentions default is ollama
    - Add `--port` argument to run_parser: type=int, default=None, help="Custom port for backend server"
    - Add `--model-path` argument to run_parser: type=str, default=None, help="Path to GGUF model file (required for llama-cpp)"
    - Update parser description from "Benchmark your Ollama models" to "Benchmark your local LLM models"

    **Update _handle_run() in cli.py:**
    - Replace `backend = create_backend()` with `backend = create_backend(args.backend, port=args.port)`
    - Pass `model_path=args.model_path` to backend if backend is llama-cpp (store on backend instance or pass to preflight)

    **Update no-args (menu) path in main():**
    - Keep `create_backend()` with default "ollama" for now (menu integration in Plan 05 will handle backend selection)

    **Update _handle_info() in cli.py:**
    - Accept optional `--backend` flag for info command too, default "ollama"
    - Create backend using args.backend if provided

    **Update tests/test_cli.py:**
    - Test that --backend flag is parsed correctly for all three values
    - Test that --port flag is parsed as int
    - Test that --model-path flag is parsed as string
    - Test default backend is "ollama"
    - Test create_backend("llama-cpp") returns LlamaCppBackend
    - Test create_backend("lm-studio") returns LMStudioBackend
    - Test create_backend("invalid") raises ValueError
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -x -v</automated>
  </verify>
  <done>
    - `python -m llm_benchmark run --backend llama-cpp` parses correctly
    - `python -m llm_benchmark run --backend lm-studio --port 5555` parses correctly
    - create_backend() supports all three backends with optional host/port
    - Existing CLI behavior unchanged (default is ollama)
  </done>
</task>

<task type="auto">
  <name>Task 2: Generalize preflight checks for any backend</name>
  <files>llm_benchmark/preflight.py, tests/test_preflight.py</files>
  <action>
    **Refactor preflight.py:**

    Replace `check_ollama_installed()` with `check_backend_installed(backend_name: str) -> bool`:
    - Use detection module: `from llm_benchmark.backends.detection import detect_backends, get_install_instructions, auto_start_backend`
    - Find the BackendStatus for the requested backend_name
    - If installed and running: return True
    - If installed but NOT running: prompt user "X is installed but not running. Start it? (y/N)" per CONTEXT.md decision
      - For llama-cpp: this prompt should note that model selection is needed first (the caller should have model_path ready)
      - If user says yes: call `auto_start_backend(backend_name, ...)`, return True on success
      - If user says no or auto-start fails: return False
    - If NOT installed: show platform-specific install instructions from `get_install_instructions()`, return False

    **Update check_backend_connectivity() error messages:**
    - Replace hardcoded "Cannot connect to Ollama" with `f"Cannot connect to {backend.name}"`
    - Replace hardcoded Ollama start instructions with backend-specific instructions:
      - ollama: "ollama serve"
      - llama-cpp: "llama-server -m <model> --port 8080"
      - lm-studio: "lms server start"

    **Update check_available_models() hint message:**
    - Replace hardcoded "ollama pull llama3.2:1b" with backend-appropriate suggestion:
      - ollama: "ollama pull llama3.2:1b"
      - llama-cpp: "Download a GGUF model from huggingface.co"
      - lm-studio: "Load a model in LM Studio UI or via: lms load <model>"

    **Update run_preflight_checks():**
    - Add `backend_name: str = "ollama"` parameter (derived from backend.name)
    - Replace `check_ollama_installed()` call with `check_backend_installed(backend.name)`
    - Keep existing chain: install (blocking) -> connectivity (blocking) -> models (blocking) -> RAM (advisory)
    - Preflight runs ONLY for the selected backend per CONTEXT.md decision

    **Update tests/test_preflight.py:**
    - Test check_backend_installed() for each backend (installed+running, installed+not-running, not-installed)
    - Test auto-start permission prompt (mock input)
    - Test backend-specific error messages in check_backend_connectivity
    - Test backend-specific model hints in check_available_models
    - Ensure existing Ollama preflight tests still pass (backward compat)
  </action>
  <verify>
    <automated>uv run pytest tests/test_preflight.py tests/test_cli.py -x -v</automated>
  </verify>
  <done>
    - Preflight works for all three backends with appropriate install/start messages
    - Auto-start prompts user for permission before starting
    - Backend-specific hints shown when no models found
    - Existing Ollama-only behavior preserved when --backend not specified
    - All 170+ existing tests pass
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/ -x -q
# All tests pass including new CLI and preflight tests
# Manual smoke test: python -m llm_benchmark run --help shows --backend, --port, --model-path flags
</verification>

<success_criteria>
- `--backend llama-cpp` creates LlamaCppBackend and runs backend-specific preflight
- `--backend lm-studio --port 5555` creates LMStudioBackend on custom port
- Default (no --backend) creates OllamaBackend with zero behavior change
- Preflight install check uses detection module for any backend
- Auto-start asks permission, polls health, captures logs
- Error messages are backend-specific (not hardcoded Ollama)
</success_criteria>

<output>
After completion, create `.planning/phases/06-new-backends/06-03-SUMMARY.md`
</output>
