---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - llm_benchmark/system.py
  - llm_benchmark/runner.py
  - llm_benchmark/exporters.py
  - llm_benchmark/compare.py
  - tests/test_runner.py
  - tests/test_system.py
autonomous: true
requirements: [STAB-01, STAB-04, STAB-05, STAB-06]

must_haves:
  truths:
    - "Benchmark runner executes a single prompt against a model and returns a BenchmarkResult"
    - "Model offloading calls ollama.generate with keep_alive=0 (no sudo)"
    - "Timeouts use threading (no signal.SIGALRM) for cross-platform compatibility"
    - "System info detects CPU, RAM, GPU across macOS/Linux/Windows"
    - "Exporters write JSON, CSV, and Markdown to a specified directory"
  artifacts:
    - path: "llm_benchmark/runner.py"
      provides: "Benchmark execution, timeout wrapper, model offloading, correct averaging"
      exports: ["run_single_benchmark", "unload_model", "run_with_timeout"]
      min_lines: 80
    - path: "llm_benchmark/system.py"
      provides: "Cross-platform hardware/system info collection"
      exports: ["get_system_info"]
      min_lines: 50
    - path: "llm_benchmark/exporters.py"
      provides: "JSON, CSV, Markdown result writers"
      exports: ["export_json", "export_csv", "export_markdown"]
    - path: "llm_benchmark/compare.py"
      provides: "Results comparison logic migrated from compare_results.py"
      exports: ["compare_results", "load_json_results"]
    - path: "tests/test_runner.py"
      provides: "Tests for runner logic"
    - path: "tests/test_system.py"
      provides: "Tests for system info collection"
  key_links:
    - from: "llm_benchmark/runner.py"
      to: "llm_benchmark/models.py"
      via: "imports BenchmarkResult, OllamaResponse"
      pattern: "from llm_benchmark\\.models import"
    - from: "llm_benchmark/runner.py"
      to: "ollama"
      via: "ollama.chat() for benchmarking, ollama.generate() for offloading"
      pattern: "ollama\\.(chat|generate)"
    - from: "llm_benchmark/exporters.py"
      to: "llm_benchmark/models.py"
      via: "imports ModelSummary, SystemInfo"
      pattern: "from llm_benchmark\\.models import"
---

<objective>
Migrate core benchmark logic from extended_benchmark.py into the package modules: runner (execution + timeouts + offloading), system info collection, exporters, and compare. This is the computational heart of the tool.

Purpose: The runner implements the actual benchmarking; system.py provides hardware detection; exporters save results. All use the data models from Plan 01.
Output: Working benchmark execution engine with tests, cross-platform system info, and result export.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@extended_benchmark.py
@compare_results.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create runner.py with benchmark execution, timeouts, and offloading</name>
  <files>llm_benchmark/runner.py, tests/test_runner.py</files>
  <behavior>
    - run_single_benchmark(model, prompt, verbose, timeout) returns BenchmarkResult with success=True on success
    - run_single_benchmark returns BenchmarkResult with success=False and error message on failure
    - run_with_timeout(func, timeout) raises TimeoutError after timeout seconds (uses threading, not signals)
    - unload_model(model_name) calls ollama.generate(model=name, prompt="", keep_alive=0)
    - compute_averages(results) returns correct total_tokens/total_time averages (not arithmetic mean of rates)
    - compute_averages excludes prompt-cached results from prompt_eval averages
  </behavior>
  <action>
1. Create `tests/test_runner.py`:
   - `test_correct_averaging`: Given 2 BenchmarkResults with known tokens/durations, verify compute_averages returns sum_tokens/sum_time (not mean of rates). Use the example from RESEARCH.md.
   - `test_correct_averaging_excludes_cached`: Given results where one has prompt_cached=True, verify prompt_eval average excludes it.
   - `test_timeout_threading`: Verify run_with_timeout raises TimeoutError for a function that sleeps longer than timeout. Confirm no signal.SIGALRM usage.
   - `test_unload_model`: Mock ollama.generate, verify it's called with keep_alive=0.
   - `test_run_single_benchmark_success`: Mock ollama.chat, verify BenchmarkResult.success=True with parsed response.
   - `test_run_single_benchmark_failure`: Mock ollama.chat to raise, verify BenchmarkResult.success=False with error.

2. Create `llm_benchmark/runner.py`:
   - `run_with_timeout(func, args, timeout_seconds)`: Use threading.Thread + join(timeout) pattern from extended_benchmark.py (~lines 188-230). Return result or raise TimeoutError. Do NOT use signal.SIGALRM (STAB-06).
   - `run_single_benchmark(model_name, prompt, verbose=False, timeout=300) -> BenchmarkResult`: Call ollama.chat() (streaming if verbose, printing chunks via rich console). Parse response into OllamaResponse. Return BenchmarkResult. Catch exceptions and return BenchmarkResult(success=False, error=str(e)).
   - `unload_model(model_name) -> bool`: Call ollama.generate(model=model_name, prompt="", keep_alive=0). Return True/False (STAB-05).
   - `compute_averages(results: list[BenchmarkResult]) -> dict`: Implement STAB-04 correct averaging -- sum all tokens, sum all durations, divide once. Exclude prompt_cached results from prompt_eval calculation. Return dict with prompt_eval_ts, response_ts, total_ts keys.
   - `benchmark_model(model_name, prompts, verbose, timeout, runs_per_prompt) -> ModelSummary`: Orchestrate running all prompts with runs_per_prompt iterations, call compute_averages, return ModelSummary.
   - Use `from llm_benchmark.config import get_console` for all output.

3. Run tests.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && python -m pytest tests/test_runner.py -x -q</automated>
  </verify>
  <done>Runner executes benchmarks with threading-based timeouts (STAB-06), offloads models via keep_alive=0 (STAB-05), and computes correct averages (STAB-04). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create system.py, exporters.py, and compare.py</name>
  <files>llm_benchmark/system.py, llm_benchmark/exporters.py, llm_benchmark/compare.py, tests/test_system.py</files>
  <action>
1. Create `llm_benchmark/system.py`:
   - Migrate system info collection from extended_benchmark.py (the `get_system_info()` function and related helpers ~lines 69-186).
   - `get_system_info() -> SystemInfo`: Detect CPU model, RAM total, GPU model/VRAM, OS, Python version, Ollama version.
   - Cross-platform: use platform module + subprocess calls to sysctl (macOS), /proc (Linux), wmic (Windows). Do NOT add psutil dependency.
   - `get_ollama_version() -> str`: Run `ollama --version` via subprocess, parse output.
   - `format_system_summary() -> str`: Return compact one-line summary for pre-benchmark display (e.g., "CPU | 16 GB RAM | GPU | Ollama 0.6.1"). Use rich markup.

2. Create `tests/test_system.py`:
   - `test_get_system_info_returns_model`: Verify get_system_info() returns a SystemInfo with non-empty cpu and ram_gb > 0. (This is an integration test that runs on the current platform.)
   - `test_format_system_summary`: Verify it returns a non-empty string.

3. Create `llm_benchmark/exporters.py`:
   - Migrate from extended_benchmark.py export functions (~lines 300-420).
   - `export_json(results, system_info, output_dir) -> Path`: Write benchmark results as JSON.
   - `export_csv(results, system_info, output_dir) -> Path`: Write CSV with one row per model per prompt.
   - `export_markdown(results, system_info, output_dir) -> Path`: Write Markdown report with system info and results table.
   - All functions create `output_dir` if it doesn't exist (default: `results/`).
   - Use timestamped filenames: `benchmark_YYYYMMDD_HHMMSS.{ext}`.

4. Create `llm_benchmark/compare.py`:
   - Migrate from compare_results.py: `load_json_results(path) -> dict` and `compare_results(files, labels) -> None` (prints comparison table).
   - Replace raw print() calls with rich Console output (tables, styled text).
   - This becomes the handler for the `compare` subcommand.

5. Run tests.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && python -m pytest tests/test_system.py tests/test_runner.py -x -q && python -c "from llm_benchmark.exporters import export_json, export_csv, export_markdown; from llm_benchmark.compare import load_json_results; print('OK')"</automated>
  </verify>
  <done>System info detects hardware cross-platform (STAB-01). Exporters write JSON/CSV/Markdown. Compare module migrated with rich output. Tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_runner.py tests/test_system.py -x -q` all green
- `python -c "from llm_benchmark.runner import run_single_benchmark, unload_model, compute_averages"` succeeds
- `python -c "from llm_benchmark.system import get_system_info, format_system_summary"` succeeds
- `python -c "from llm_benchmark.exporters import export_json"` succeeds
- No usage of signal.SIGALRM anywhere in the package
</verification>

<success_criteria>
- Runner executes benchmarks with correct averaging (STAB-04)
- Model offloading uses keep_alive=0 API (STAB-05)
- Timeouts use threading, not signals (STAB-06)
- System info works on current platform (STAB-01)
- All modules importable and tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
