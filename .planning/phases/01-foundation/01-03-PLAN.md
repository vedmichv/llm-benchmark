---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - llm_benchmark/__main__.py
  - llm_benchmark/cli.py
  - llm_benchmark/preflight.py
  - run.py
  - tests/test_preflight.py
  - tests/test_cli.py
  - CLAUDE.md
autonomous: true
requirements: [STAB-02, STAB-03]

must_haves:
  truths:
    - "python -m llm_benchmark --help shows subcommands (run, compare, info)"
    - "If Ollama is not running, the tool prints platform-specific start instructions (not a stack trace)"
    - "If no models are found, the tool suggests pulling llama3.2:1b"
    - "If a model may exceed available RAM, a warning is shown but execution continues"
    - "run.py is a thin wrapper that calls python -m llm_benchmark"
    - "Old files (benchmark.py, extended_benchmark.py, run.sh, run.bat, run.ps1, setup_passwordless_sudo.sh, requirements.txt) are removed"
  artifacts:
    - path: "llm_benchmark/__main__.py"
      provides: "Entry point for python -m llm_benchmark"
      contains: "__main__"
      min_lines: 10
    - path: "llm_benchmark/cli.py"
      provides: "Argparse CLI with run/compare/info subcommands"
      exports: ["main"]
      min_lines: 60
    - path: "llm_benchmark/preflight.py"
      provides: "Ollama connectivity check, model availability, RAM warnings"
      exports: ["run_preflight_checks"]
      min_lines: 50
    - path: "tests/test_preflight.py"
      provides: "Tests for preflight checks"
    - path: "tests/test_cli.py"
      provides: "Tests for CLI argument parsing"
  key_links:
    - from: "llm_benchmark/__main__.py"
      to: "llm_benchmark/cli.py"
      via: "imports and calls main()"
      pattern: "from llm_benchmark\\.cli import main"
    - from: "llm_benchmark/cli.py"
      to: "llm_benchmark/runner.py"
      via: "run subcommand dispatches to benchmark_model"
      pattern: "from llm_benchmark\\.runner import"
    - from: "llm_benchmark/cli.py"
      to: "llm_benchmark/preflight.py"
      via: "run subcommand calls preflight before benchmarking"
      pattern: "from llm_benchmark\\.preflight import"
    - from: "llm_benchmark/preflight.py"
      to: "ollama"
      via: "ollama.list() for connectivity and model checks"
      pattern: "ollama\\.list\\(\\)"
---

<objective>
Wire up the CLI entry points, pre-flight checks, and clean up old files. After this plan, `python -m llm_benchmark run` is the single way to use the tool.

Purpose: This connects all modules into a working CLI application with pre-flight safety checks. Old scripts are removed to eliminate confusion (QUAL-01).
Output: Complete working CLI tool with pre-flight checks. Old files deleted.
</objective>

<execution_context>
@/Users/viktor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/viktor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create preflight checks with Ollama connectivity and RAM warnings</name>
  <files>llm_benchmark/preflight.py, tests/test_preflight.py</files>
  <behavior>
    - check_ollama_connectivity() returns True when ollama.list() succeeds
    - check_ollama_connectivity() returns False and prints platform-specific guidance when ollama.list() raises
    - check_available_models() returns model list when models exist
    - check_available_models() returns empty list and suggests "ollama pull llama3.2:1b" when no models
    - check_ram_for_models(models) warns for models estimated to exceed 80% of available RAM, but does not block
    - run_preflight_checks() chains all checks: connectivity (blocking) -> models (blocking) -> RAM (warning only)
  </behavior>
  <action>
1. Create `tests/test_preflight.py`:
   - `test_ollama_unreachable`: Mock ollama.list to raise Exception. Verify check_ollama_connectivity() returns False. Capture rich output and verify it contains platform-specific instruction (use platform.system() to pick expected string).
   - `test_ollama_reachable`: Mock ollama.list to return a response. Verify returns True.
   - `test_no_models_found`: Mock ollama.list to return empty models list. Verify check_available_models() returns empty list and output contains "ollama pull llama3.2:1b".
   - `test_ram_warning`: Mock system RAM to 8GB, create a mock model with size=7GB. Verify check_ram_for_models warns but returns (does not raise/exit).
   - `test_ram_ok`: Mock system RAM to 32GB, model size=4GB. Verify no warning.

2. Create `llm_benchmark/preflight.py`:
   - Follow Pattern 2 from RESEARCH.md.
   - `check_ollama_connectivity() -> bool`: Try ollama.list(). On failure, print rich Panel with platform-specific instructions (macOS: "ollama serve", Windows: "Start the Ollama application", Linux: "ollama serve"). Include download link.
   - `check_available_models(skip_models=None) -> list`: Call ollama.list(), filter by skip list. If empty, suggest "ollama pull llama3.2:1b".
   - `check_ram_for_models(models)`: For each model, estimate RAM as model.size * RAM_SAFETY_MULTIPLIER (from config). Compare to system RAM. Warn via rich if estimated > 80% of available RAM. Use console.print with yellow warning. This warns but CONTINUES -- per user decision "don't gatekeep".
   - `run_preflight_checks(skip_models=None, skip_checks=False) -> list[Model]`: Chain checks. If skip_checks, skip RAM check only. Return filtered model list. Sys.exit(1) if Ollama unreachable or no models.
   - Use `from llm_benchmark.config import get_console` for all output.

3. Run tests.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && python -m pytest tests/test_preflight.py -x -q</automated>
  </verify>
  <done>Pre-flight checks work: Ollama connectivity check blocks with platform guidance (STAB-02), RAM check warns without blocking (STAB-03). Tests pass with mocked Ollama.</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI, entry points, update run.py, and remove old files</name>
  <files>llm_benchmark/cli.py, llm_benchmark/__main__.py, run.py, tests/test_cli.py, CLAUDE.md</files>
  <action>
1. Create `llm_benchmark/cli.py`:
   - Follow Pattern 4 from RESEARCH.md.
   - `main(argv=None) -> int`: Create argparse parser with `--debug` global flag.
   - Subcommands:
     a. `run`: --verbose/-v, --skip-checks, --skip-models (nargs=*), --prompt-set (choices: small/medium/large, default: medium), --prompts (nargs=*, custom prompts override prompt-set), --runs-per-prompt (default: 2), --timeout (default: 300)
     b. `compare`: files (nargs=+), --labels (nargs=*)
     c. `info`: no additional args (prints full system info)
   - Dispatch:
     - `run`: Call set_debug(args.debug), run_preflight_checks(), print system summary, then loop over models calling benchmark_model(). After each model, call unload_model(). Export results. On model failure: ask "Continue with remaining models? [Y/n]" via input().
     - `compare`: Call compare_results from compare module.
     - `info`: Call get_system_info() and print full details via rich.
   - Return 0 on success, 1 on failure.

2. Create `llm_benchmark/__main__.py`:
   - Follow Pattern 1 from RESEARCH.md.
   - Wrap cli.main() in try/except. On KeyboardInterrupt: print "Benchmark interrupted", exit 130. On Exception: if debug mode, re-raise; otherwise print friendly error with rich and suggest --debug.

3. Create `tests/test_cli.py`:
   - `test_run_subcommand_parsing`: Verify `main(["run"])` parses without error (mock the actual execution).
   - `test_compare_subcommand_parsing`: Verify `main(["compare", "file1.json", "file2.json"])` parses.
   - `test_info_subcommand_parsing`: Verify `main(["info"])` parses.
   - `test_help_shows_subcommands`: Capture --help output, verify "run", "compare", "info" appear.
   - `test_debug_flag`: Verify --debug sets debug mode.

4. Update `run.py` to be a thin wrapper:
   ```python
   #!/usr/bin/env python3
   """Convenience launcher. Prefer: python -m llm_benchmark"""
   import subprocess, sys
   sys.exit(subprocess.call([sys.executable, "-m", "llm_benchmark"] + sys.argv[1:]))
   ```

5. Remove old files that are now redundant:
   - `benchmark.py` (migrated to llm_benchmark/models.py + runner.py)
   - `extended_benchmark.py` (migrated to llm_benchmark/ package)
   - `compare_results.py` (migrated to llm_benchmark/compare.py)
   - `run.sh`, `run.bat`, `run.ps1` (run.py covers all platforms)
   - `setup_passwordless_sudo.sh` (obsolete, keep_alive=0 API replaces it)
   - `requirements.txt` (replaced by pyproject.toml)
   - `test_ollama.py` (replaced by tests/ directory)

6. Update `CLAUDE.md` to reflect new package structure:
   - Change Python version to 3.12+
   - Update setup to use `uv sync` or `pip install -e .`
   - Update running instructions to `python -m llm_benchmark run`
   - Update architecture section to describe package layout
   - Remove references to benchmark.py and extended_benchmark.py

7. Run all tests.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/llm-benchmark && python -m pytest tests/ -x -q && python -m llm_benchmark --help</automated>
  </verify>
  <done>CLI works with run/compare/info subcommands. __main__.py catches exceptions gracefully. run.py is a thin wrapper. Old files removed (QUAL-01). CLAUDE.md updated. All tests pass. `python -m llm_benchmark --help` shows subcommands.</done>
</task>

</tasks>

<verification>
- `python -m llm_benchmark --help` shows run, compare, info subcommands
- `python -m llm_benchmark run --help` shows all run flags
- `python -m pytest tests/ -x -q` all green
- Old files (benchmark.py, extended_benchmark.py, compare_results.py, run.sh, run.bat, run.ps1, setup_passwordless_sudo.sh, requirements.txt, test_ollama.py) do not exist
- run.py is less than 10 lines
- CLAUDE.md references new package structure
</verification>

<success_criteria>
- `python -m llm_benchmark run` with Ollama running: executes pre-flight, shows system summary, runs benchmark
- `python -m llm_benchmark run` without Ollama: shows friendly error with platform-specific start instructions (STAB-02)
- RAM warning appears for large models but doesn't block (STAB-03)
- Single consolidated module, no duplicate scripts (QUAL-01)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
